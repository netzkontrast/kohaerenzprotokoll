# dspy-agent-skills — the BOOK part (chapters 1–11 of "Context Engineering with DSPy") and `articles/`

## 1. Header

- **Repo:** `/home/user/dspy-agent-skills` = github.com/netzkontrast/dspy-agent-skills (fork of intertwine/dspy-agent-skills), commit `9d13f98` (9d13f981d276…, 2026-09-16), working tree clean. Plugin version `0.11.0` (`.claude-plugin/plugin.json`). **License:** MIT (`LICENSE`, © 2026 Bryan Young); the skills say the underlying book repo `context-engineering-dspy-book` is also MIT.
- **Slice read in full (32 files):** `skills/dspy-book-{eight-steps,datasets,metrics,optimizers,modules,agents,use-cases,production,coding-agents}/{SKILL.md,reference.md,example_*.py}` and `articles/{README,01..04}.md`. All paths below are relative to the repo root unless they start with `dspy/` or `gepa/`, which are relative to `/home/user/kohaerenzprotokoll/.venv-dspy/lib/python3.11/site-packages/`.
- **DSPy targeted:** the skills say the book pins `dspy==3.3.0` (e.g. `skills/dspy-book-eight-steps/reference.md:3-4`), `gepa==0.1.1` (`skills/dspy-book-coding-agents/reference.md:3-4`), `mlflow==3.15.1` (`skills/dspy-book-production/reference.md:3-4`). **Against 3.3.1 the API usage mostly holds, with these breaks, all verified:** `dspy.load(dir)` now needs `allow_pickle=True`; `from dspy.datasets import GSM8K` fails; `dspy.BAMLAdapter` is not a top-level name; `dspy.Image("<local path>")` raises and `Image.from_url` is not deprecated; the installed gepa is **0.1.4** — DSPy 3.3.1 itself requires `gepa[dspy]==0.1.4` — not 0.1.1. Several behavioural claims are wrong or incomplete on 3.3.1 (MCP sync call, `Parallel` with dicts, "stacks demos", JSON adapter selection) — see `## TRAP`.
- **What it is:** nine documentation-as-skill files, each distilling one or more book chapters (1–3 method, 4 data, 5 metrics, 6 optimizer results, 7 modules/adapters, 8 agents, 9 applications, 10 serving/tracing, 11 optimizing text artifacts with `gepa.optimize_anything`), each with a `reference.md` and a `--dry-run` example that encodes the chapter's rules as plain-Python checks. The four articles are the upstream author's blog posts about the original five-skill v0.1.0 pack and its three GEPA examples.
- **What I ran (all offline, keys unset with `env -u OPENROUTER_API_KEY -u TYPESAFE_API_KEY -u OPENAI_API_KEY -u ANTHROPIC_API_KEY`, interpreter `/home/user/kohaerenzprotokoll/.venv-dspy/bin/python` = DSPy 3.3.1, gepa 0.1.4, Python 3.11.15):**
  1. all nine `example_*.py --dry-run` → all print `dry run OK`, exit 0 (none of them imports `dspy`, see TEST-2);
  2. `inspect.signature` of every DSPy class/function the slice cites (`verify/sigs.py`);
  3. 23 behaviour probes in eight scripts with `dspy.utils.dummies.DummyLM` and a hand-written `BaseLM` (`verify/behave1.py … behave6.py`, `behave2b.py`, `boot1.py`), a full offline `dspy.GEPA.compile` with DummyLM task and reflection LMs (`verify/gepa1.py`), and full offline `gepa.optimize_anything` runs with a Python callable as reflection LM (`verify/oa1.py … oa3.py`);
  4. arithmetic of the chapter-6 table and a recomputation of its McNemar p (`verify/table.py`);
  5. a cross-check of article 3's numbers against the committed `examples/*/results.json` and `version_comparison.md` (outside the slice, read only to check the articles).
  Verification scripts live in `<scratchpad>/verify/`. Nothing in any repository was modified. mcp, langchain-core, mlflow, soundfile and Pillow are not installed, so MCP/MLflow/audio claims are checked by reading source only, or marked `[claim]`.
- **Numbers from the book itself** (chapter tables, costs, times) cannot be re-run here — the book repo is not cloned — so they carry `[number]` with the skill as the source; where the arithmetic could be checked it was, and it is internally consistent.

Item IDs (`API-7`, `OPT-3` …) are used by the last two sections.

## API

- **API-1 dual-mode metric signature** — `metric(example, pred, trace=None, pred_name=None, pred_trace=None)`; return a plain number when `pred_name` is falsy (plain evaluation), `dspy.Prediction(score=..., feedback=...)` when an optimizer asks. „Write the five-argument form from the start. It costs nothing and keeps reflective optimization available." `skills/dspy-book-eight-steps/SKILL.md:49-63`, `skills/dspy-book-eight-steps/reference.md:35-44`, `skills/dspy-book-datasets/reference.md:66-75` [recipe] (verified: `dspy.Evaluate` calls the metric as `metric(example, prediction)` — trace, pred_name and pred_trace all `None`, `dspy/evaluate/evaluate.py:172`; behave1.py T4) → here: `pairs.py` metric.
- **API-2 who calls the metric with which arguments (3.3.1)** — `Evaluate`: `(example, pred)`. `BootstrapFewShot` (demo acceptance): `(example, pred, trace)` with a real trace list and `pred_name=None` (`dspy/teleprompt/bootstrap.py:206`). `dspy.GEPA` module-level scoring: `trace=None, pred_name=None`; per-predictor feedback: `metric(module_inputs, module_outputs, captured_trace, pred_name, [(predictor, predictor_inputs, predictor_output)])` — `pred_trace` is a one-element list, and `pred_name` is `"self"` for a bare `Predict` (`dspy/teleprompt/gepa/gepa.py:584-605`). The slice never states this matrix [api] (verified: boot1.py call shapes `{('trace', None)}`; gepa1.py call shapes `('none', None, 'none')` ×32 and `('trace', 'self', 'pt')` ×15) → here: `pairs.py` — branch on `pred_name`, see TRAP-16.
- **API-3 metric return types and `sum()`** — `dspy.Evaluate` aggregates with `sum(score for *_, score in results)`; a dict return raises `TypeError: unsupported operand type(s) for +: 'int' and 'dict'` — only at aggregation, after every program call has been paid for; a `dspy.Prediction` return works because `Prediction` converts through its `score` field; a `Prediction` without `score` raises `ValueError: Prediction object does not have a 'score' field to convert to float.` `skills/dspy-book-metrics/reference.md:8-9`, `skills/dspy-book-eight-steps/example_eight_steps.py:67-73`, `articles/01-why-dspy-agent-skills.md:56` [api] (verified behave1.py T1/T3; `dspy/evaluate/evaluate.py:180`).
- **API-4 `EvaluationResult.score` is 0–100** — `score=round(100 * ncorrect / ntotal, 2)`; `Evaluate.__call__` returns an `EvaluationResult` (a `Prediction` subclass with `.score` and `.results` = list of `(example, prediction, score)`); „always take `.score`". `skills/dspy-book-eight-steps/reference.md:97`, `skills/dspy-book-production/reference.md:107-108` [api] (verified: 2 of 4 correct → `50.0`; `dspy/evaluate/evaluate.py:224`).
- **API-5 `Evaluate` constructor (3.3.1)** — `Evaluate(*, devset, metric=None, num_threads=None, display_progress=False, display_table=False, max_errors=None, provide_traceback=None, failure_score=0.0, save_as_csv=None, save_as_json=None)`; `return_outputs` is gone (`ValueError`). Human labelling runs through it with `num_threads=1` („CRITICAL" in the chapter, or interactive prompts interleave). `skills/dspy-book-metrics/reference.md:115-119` [api] (verified signature).
- **API-6 BestOfN / Refine constructor and reward contract** — both `(module, N, reward_fn, threshold, fail_count=None)`; `reward_fn(kwargs, pred)` where `kwargs` is the dict of forward inputs — not the metric's argument list. „The calling-convention mismatch is real and documented". `skills/dspy-book-eight-steps/SKILL.md:72-74`, `skills/dspy-book-eight-steps/reference.md:65-75`, `skills/dspy-book-modules/reference.md:103-106`, `skills/dspy-book-use-cases/reference.md:99-100` [api] (verified: reward_fn received `{'text': 'hello'}`, behave1.py T5; `dspy/predict/best_of_n.py:67`, `dspy/predict/refine.py:137`).
- **API-7 BestOfN sampling mechanics** — each attempt deep-copies the module and sets `lm.copy(rollout_id=start+i, temperature=1.0)`, so attempts bypass the cache and ignore your temperature; returns the first attempt whose reward ≥ `threshold`, else the highest-reward one; `fail_count` defaults to `N`. [api] (verified `dspy/predict/best_of_n.py:48-73`).
- **API-8 Refine is BestOfN plus an LLM critique** — after every below-threshold attempt except the last, Refine calls `dspy.Predict(OfferFeedback)` (an extra LM call on the *global* LM) with the module's source, `inspect.getsource(reward_fn)`, the trajectory and the reward, and injects the per-module advice into the next attempt as an extra input field `hint_`. The skill's one-line summary („Refine generates targeted feedback for the next retry") is right; the cost and what is sent are not stated. `skills/dspy-book-modules/reference.md:103-106` [api] (verified `dspy/predict/refine.py:92-96,121-130,142-167`).
- **API-9 adapters are explicit, global and sticky** — `dspy.configure(lm=lm, adapter=dspy.JSONAdapter())`; `assert isinstance(dspy.settings.adapter, dspy.JSONAdapter)`; debug with `dspy.inspect_history(n=1)` (signature `inspect_history(n=1, file=None)`). `skills/dspy-book-modules/SKILL.md:24-30,48`, `skills/dspy-book-modules/reference.md:7-9` [api] (verified signatures).
- **API-10 adapter table** — `ChatAdapter` (default; `[[ ## field_name ## ]]` markers), `JSONAdapter` (JSON schema from the Pydantic type, structured-output mode where supported), `XMLAdapter` (`<field>content</field>`, extends ChatAdapter), `TwoStepAdapter` (two calls). 3.3.1 constructors: `ChatAdapter(callbacks=None, use_native_function_calling=False, native_response_types=None, use_json_adapter_fallback=True, parallel_tool_calls=None)`, `JSONAdapter(callbacks=None, use_native_function_calling=True, parallel_tool_calls=None)`, `XMLAdapter` same as ChatAdapter. `skills/dspy-book-modules/SKILL.md:32-37`, `skills/dspy-book-modules/reference.md:16-21` [api] (verified signatures).
- **API-11 ChatAdapter falls back to JSONAdapter by itself** — on any exception except `LMError`, the default ChatAdapter re-runs the call through `JSONAdapter` (a second provider request). So „DSPy does not select JSONAdapter automatically for a Pydantic output type" (`skills/dspy-book-modules/SKILL.md:39-42`) is true only for the first attempt. [api] (verified behave3.py T12: an unparseable first reply gave a parsed Pydantic record after **2** LM calls; with `ChatAdapter(use_json_adapter_fallback=False)` → `AdapterParseError` after 1 call; `dspy/adapters/chat_adapter.py:47,85-95`) → here: `lmrun.py` — one predictor call can be two requests; set the fallback off to see `unparsed`.
- **API-12 TwoStepAdapter** — `dspy.TwoStepAdapter(extraction_model=dspy.LM("<small-model>"))` with a reasoning main LM (`temperature=1.0, max_tokens=16000` in the chapter): unconstrained generation, then a small model extracts the fields. `extraction_model` must be a `dspy.BaseLM` (`ValueError`). DSPy's own source: the second step's signature is built on the fly with a demo-less ChatAdapter, „This means that it cannot "learn" or get optimized." `skills/dspy-book-modules/SKILL.md:44-46`, `skills/dspy-book-modules/reference.md:11-13` [api] (verified `dspy/adapters/two_step_adapter.py:17-18,46`).
- **API-13 BAMLAdapter location** — `dspy.BAMLAdapter` does not exist as a top-level name in 3.3.1; `from dspy.adapters.baml_adapter import BAMLAdapter` works (subclass of `JSONAdapter`). The skill says „`dspy.BAMLAdapter` ships in DSPy 3.x". `skills/dspy-book-modules/reference.md:23` [api] (verified `hasattr(dspy,'BAMLAdapter') == False`, `dspy/adapters/baml_adapter.py:163`).
- **API-14 custom adapter hooks** — subclass `ChatAdapter`, override `format_field_with_value(self, fields_with_values: dict[FieldInfoWithName, Any]) -> str`, `user_message_output_requirements(self, signature) -> str`, `parse(self, signature, completion) -> dict[str, Any]`; imports `from dspy.adapters.chat_adapter import ChatAdapter, FieldInfoWithName` and `from dspy.adapters.utils import format_field_value, parse_value`; subclass `Adapter` for full control of message structure. `skills/dspy-book-modules/SKILL.md:49-50`, `skills/dspy-book-modules/reference.md:27-37` [api] (verified: imports resolve, hook signatures match on 3.3.1).
- **API-15 LM history is the prompt export** — `dspy.settings.lm.history[-1]["messages"]` is plain chat format; a history entry's keys in 3.3.1: `cost, kwargs, messages, model, model_type, outputs, prompt, response, response_model, timestamp, usage, uuid`; history is capped by `max_history_size=10000`. `skills/dspy-book-production/SKILL.md:77-84`, `skills/dspy-book-production/reference.md:57-60` [api] (verified behave2b.py; `dspy/dsp/utils/settings.py:35`).
- **API-16 `dspy.Image` in 3.3.1** — constructor takes one positional source: HTTP(S)/GS URL (kept as a URL reference, not downloaded), data URI, raw bytes (needs Pillow), a PIL image, or `{"url": ...}`. A local path string raises `ValueError: Unrecognized image string: … Local files must be loaded with Image.from_path().` `Image.from_url(url, verify=True, timeout=30.0)` is the explicit download path (no SSRF protection: „will reach private, loopback, or cloud-metadata hosts"); `Image.from_file` is deprecated for `Image.from_path` and `Image.from_PIL` for `Image(pil)`, removal „in 3.4"; the constructor's `download=`/`verify=` are deprecated. The skill says the constructor accepts a path and that `from_url` is deprecated — both wrong for 3.3.1. `skills/dspy-book-modules/SKILL.md:55,62-64`, `skills/dspy-book-modules/reference.md:42,47-48` [api] (verified `dspy/adapters/types/image.py:116-153,206`; behave4.py T14).
- **API-17 `dspy.Audio`** — `Audio.from_array(array, sampling_rate, format="wav")`, `from_file`, `from_url` all exist and are not deprecated; `from_array` needs the `soundfile` package (`ImportError: soundfile is required to process audio arrays.`), which the slice never mentions. „Not every provider accepts audio. The chapter's own setup uses a text-and-image model". `skills/dspy-book-modules/SKILL.md:58,63-64,70-72` [api] (verified behave4.py T14; `dspy/adapters/types/audio.py:148-153`).
- **API-18 media types inside string signatures** — use the dotted name: `"image: dspy.Image, category_options: list[str] -> category: str, confidence: float"` and `"recording: dspy.Audio, context: str -> action_items: list[str]"` parse; a bare `image: Image` raises `ValueError: Unknown name: Image`. No built-in video type; documents via the third-party `attachments` package (a `dspy.Type` subclass). `skills/dspy-book-modules/SKILL.md:56-59,66-68`, `skills/dspy-book-modules/reference.md:44,56-57` [api] (verified the three parses).
- **API-19 custom media type** — subclass `dspy.adapters.types.base_type.Type` (also exported as `dspy.Type`) and implement `format() -> list[dict] | str` returning LiteLLM content blocks; the chapter's video type sends video through the image-url block shape for some providers and reads `dspy.settings.lm.model` inside `format()` to reject models that cannot handle it. `skills/dspy-book-modules/reference.md:50-54` [api] (verified the base class and `format` exist; the video details are [claim]).
- **API-20 code-execution module constructors (3.3.1)** — `ProgramOfThought(signature, max_iters=3, interpreter_factory=PythonInterpreter)`; `CodeAct(signature, tools, max_iters=5, interpreter_factory=PythonInterpreter)`; `RLM(signature, max_iters=20, max_llm_calls=50, max_output_chars=10000, verbose=False, tools=None, sub_lm=None, interpreter_factory=PythonInterpreter)`. The chapter's calls: `dspy.ProgramOfThought("portfolio: str -> total_return: str")`, `dspy.CodeAct("question: str -> answer: str", tools=[get_population, get_temperature], max_iters=5)`, `dspy.RLM("document: str, question: str -> answer: str", max_iters=20, max_llm_calls=50, sub_lm=dspy.LM("<cheap-model>"))`. `skills/dspy-book-modules/reference.md:61-66` [api] (verified signatures).
- **API-21 instruction slots per module** — `named_predictors()`: ProgramOfThought → `code_generate.predict, code_regenerate.predict, generate_output.predict`; CodeAct → `codeact, extractor.predict`; RLM → `generate_action, extract`; ReAct → `react, extract.predict`; ChainOfThought → `predict`. Each is one GEPA instruction slot. [api] (verified behave4.py T13, behave2.py T9) → here: `rlm_ingest.py`, `pairs.py`.
- **API-22 CodeAct tool contract** — tools must pass `inspect.isfunction(tool.func)`: plain functions and lambdas are accepted; callable objects, **bound methods** and `functools.partial` raise `ValueError: CodeAct only accepts functions and not callable objects.` at construction. ReAct accepts functions, callable objects or `dspy.Tool`. `skills/dspy-book-modules/SKILL.md:82-84`, `skills/dspy-book-modules/reference.md:71-72` [api] (verified behave3.py T10; `dspy/predict/code_act.py:64-65`).
- **API-23 `dspy.majority`** — `majority(prediction_or_completions, normalize=default_normalize, field=None)` accepts a `Prediction`, `Completions` or a list; `field` defaults to the last output field; `default_normalize` = `normalize_text(s) or None` (Unicode NFD, lowercase, punctuation removed, English articles `a|an|the` removed, whitespace collapsed); completions that normalize to `None` are ignored; ties go to the earliest completion; it returns a `Prediction` built from the first completion whose normalized value won (original text). Completions come from `module(..., config={"n": 5, "temperature": 1.0})`; with `n=1` voting is a no-op. `skills/dspy-book-modules/SKILL.md:93-101`, `skills/dspy-book-modules/reference.md:78-89` [api] (verified behave2.py T7: `['NYC','nyc','New York City','Boston','NYC']` → `NYC`; `dspy/predict/aggregation.py:5-50`).
- **API-24 `dspy.Parallel` (3.3.1)** — `Parallel(num_threads=None, max_errors=None, access_examples=True, return_failed_examples=False, provide_traceback=None, disable_progress_bar=False, timeout=120, straggler_limit=3)`, called with a list of `(module, input)` pairs: an `Example` is unpacked with `example.inputs()`, a `dict` with `module(**dict)`. „Total cost is unchanged; only wall-clock time drops." `skills/dspy-book-modules/SKILL.md:103-112`, `skills/dspy-book-modules/reference.md:82-84` [api] (verified behave2.py T6; `dspy/predict/parallel.py:77-114`) — see TRAP-10 for the dict and missing-`with_inputs` cases.
- **API-25 `Module.batch`** — `module.batch(examples, num_threads=None, max_errors=None, return_failed_examples=False, provide_traceback=None, disable_progress_bar=False, timeout=120, straggler_limit=3)`, the lighter form of Parallel for one module. `skills/dspy-book-modules/SKILL.md:110-111` [api] (verified behave2.py T6).
- **API-26 `MultiChainComparison`** — `MultiChainComparison(signature, M=3, temperature=0.7, **config)` compares completions instead of needing a reward function; the skill writes `temperature=1.0`, which is the chapter's argument, not the default. `skills/dspy-book-modules/reference.md:108-109` [api] (verified signature).
- **API-27 ChainOfThought's reasoning field** — `ChainOfThought(signature, rationale_field=None, rationale_field_type=str, **config)` prepends an output field named `reasoning`; `rationale_field=` is still accepted in 3.3.1 without a warning but only changes that field's definition, not its name. A signature that already declares `rationale` yields output fields `['reasoning', 'rationale', 'answer']` — the duplicate the use-cases chapter commits. `skills/dspy-book-modules/reference.md:121-122`, `skills/dspy-book-use-cases/SKILL.md:98`, `skills/dspy-book-use-cases/reference.md:108` [api] (verified behave4.py T18 and a CoT probe; `dspy/predict/chain_of_thought.py`).
- **API-28 composition primitives** — sequential (plain kwargs forward), conditional (router predictor output drives a Python `if`), wrapping (`BestOfN(module=, N=, reward_fn=, threshold=)`), per-module LM (`self.extract.set_lm(dspy.LM(...))`, `Module.set_lm(lm)`), self-refinement (draft → critique `-> feedback: str, score: float` → revise, break on threshold; „the manual version of what `Refine` does automatically"). `skills/dspy-book-modules/SKILL.md:114-119`, `skills/dspy-book-modules/reference.md:95-101` [pattern].
- **API-29 save/load formats** — `program.save(path, save_program=False)` (state JSON) vs `save(dir, save_program=True)` (cloudpickle `program.pkl` + `metadata.json`); `dspy.load(path, allow_pickle=False)` loads the latter and **raises** unless `allow_pickle=True`; it warns on dependency-version mismatch. `Module.load(path, allow_pickle=False, allow_unsafe_lm_state=False)`, `dump_state(json_mode=True)`, `load_state(state, *, allow_unsafe_lm_state=False)`. `skills/dspy-book-eight-steps/reference.md:56-57`, `skills/dspy-book-coding-agents/reference.md:109-111` [api] (verified `dspy/utils/saving.py:27-41`; signatures) — see TRAP-3, TRAP-4.
- **API-30 `dspy.Tool`** — `Tool(func, name=None, desc=None, args=None, arg_types=None, arg_desc=None)`; name from the function name (a callable object gets its class name), description from the docstring, `args` JSON schema from type hints including defaults; a wrong-typed argument raises `ValueError: Arg year is invalid: 'abc' is not of type 'integer'`, an unknown one `ValueError: Arg town is not in the tool's args.` „Override only when needed". `skills/dspy-book-agents/reference.md:107-109` [api] (verified).
- **API-31 async tools** — `Tool.__call__` on an async function raises `ValueError: You are calling `__call__` on an async tool, please use `acall` instead or enable async-to-sync conversion with `dspy.configure(allow_tool_async_sync_conversion=True)` or `with dspy.context(allow_tool_async_sync_conversion=True):`.`; `Tool.acall` also runs sync tools. Setting default `allow_tool_async_sync_conversion=False`. `skills/dspy-book-agents/SKILL.md:38-41`, `skills/dspy-book-agents/reference.md:26-29` [api] (verified `dspy/adapters/types/tool.py:182-205`, `dspy/dsp/utils/settings.py:34`) — but see TRAP-11 for what ReAct does with it.
- **API-32 `Tool.from_mcp_tool`** — `dspy.Tool.from_mcp_tool(session, tool, *, result_mode="text")`; there is no `from_mcp`. The converted tool is an async closure calling `session.call_tool(tool.name, arguments=kwargs)`, so it only works while the session context is open; MCP `isError` → `RuntimeError("Failed to call a MCP tool: …")`; one text block → `str`, several → list; `result_mode="structured"` returns MCP structured content when present; works with mcp SDK v1 `ClientSession` and v2 `Client`. `skills/dspy-book-agents/SKILL.md:24-36`, `skills/dspy-book-agents/reference.md:8-20` [api] (verified by reading `dspy/utils/mcp.py:27-86`; `mcp` not installed, not executed).
- **API-33 `Tool.from_langchain`** — wraps `tool.ainvoke`, so it is async like MCP tools; needs `langchain-core`. `skills/dspy-book-agents/SKILL.md:43`, `skills/dspy-book-agents/reference.md:31` [api] (verified `dspy/utils/langchain_tool.py:22-24`).
- **API-34 `dspy.History`** — a frozen Pydantic model `History(messages: list[dict[str, Any]])`, `extra="forbid"`; used as `history: dspy.History = dspy.InputField()`; each message is keyed by the signature's own field names (input-field keys render as a user turn, output-field keys as an assistant turn); first turn passes `dspy.History(messages=[])`; nothing persists — the caller rebuilds it every turn. `skills/dspy-book-agents/SKILL.md:47-56`, `skills/dspy-book-agents/reference.md:35-45` [api] (verified `dspy/adapters/types/history.py:61-68`, `dspy/adapters/base.py:626-660`) — see TRAP-12.
- **API-35 ReAct (3.3.1)** — `ReAct(signature, tools, max_iters=20)` (DSPy's own docstring says „Defaults to 10" — wrong); `max_iters` can be overridden per call (`agent(question=..., max_iters=5)`); a `finish` tool is added; the trajectory is a flat dict `thought_{i}`, `tool_name_{i}`, `tool_args_{i}`, `observation_{i}` and includes the finish step (observation `"Completed."`); a tool exception becomes the observation string `Execution error in <tool>: <traceback>`; a `ValueError` from the step predictor (invalid tool choice) ends the loop with a warning; on context overflow the oldest 4 keys are dropped, up to 3 times. `skills/dspy-book-agents/reference.md:111-113` [api] (verified `dspy/predict/react.py:17-121,183-199`; behave2.py T9 trajectory keys).
- **API-36 retriever contract and `dspy.retrievers.Embeddings`** — „A retriever is any callable returning an object with `.passages`." `dspy.Embedder(model_or_callable, batch_size=200, caching=True, **kwargs)` (the chapter: `dspy.Embedder("openai/text-embedding-3-small", dimensions=512)`); `dspy.retrievers.Embeddings(corpus, embedder, k=5, callbacks=None, cache=False, brute_force_threshold=20000, normalize=True)` returns a `Prediction` with `passages` and `indices`. `skills/dspy-book-agents/SKILL.md:74-77`, `skills/dspy-book-agents/reference.md:59-65` [api] (verified behave4.py T15 with a local callable embedder).
- **API-37 `dspy.asyncify`** — runs the sync program in a worker thread through an anyio `CapacityLimiter(async_max_workers)` (setting default **8**; the chapter configures 4), copies the caller's `dspy.context` overrides into the worker, `abandon_on_cancel=True` (a cancelled request leaves the LM call running). `skills/dspy-book-production/SKILL.md:26-42` [api] (verified `dspy/utils/asyncify.py:24,63`, `dspy/dsp/utils/settings.py:22`; behave4.py T18).
- **API-38 `dspy.configure` ownership** — only the thread that first called `dspy.configure` may call it again (`RuntimeError: dspy.settings can only be changed by the thread that initially configured it.`); inside async tasks only the first-configuring task may; everyone else uses `dspy.context(...)`. Relevant to the FastAPI lifespan pattern. [api] (verified `dspy/dsp/utils/settings.py`, `_ensure_configure_allowed`).
- **API-39 `dspy.make_signature`** — `make_signature({name: (type, dspy.InputField(desc=...))}, instructions=..., signature_name="StringSignature", custom_types=None)`; the chapter's Gradio app builds signatures at runtime this way. `skills/dspy-book-production/reference.md:101-105` [api] (verified behave4.py T18).
- **API-40 `desc=` and `description=`** — both reach the prompt in 3.3.1; the skills call `desc` canonical and flag one notebook's `description=`. `skills/dspy-book-eight-steps/reference.md:96`, `skills/dspy-book-datasets/reference.md:136-137` [api] (verified behave6.py).
- **API-41 `settings.configure` vs `configure`, scoped teacher** — `dspy.settings.configure(lm=...)` still works; `dspy.configure(...)` is current; `with dspy.context(lm=teacher):` scopes a teacher pass; a teacher is made with `module.deepcopy()` then `set_lm`. `skills/dspy-book-datasets/reference.md:134-136`, `skills/dspy-book-production/reference.md:107-108` [api].
- **API-42 usage tracking** — `dspy.configure(track_usage=True)` → `pred.get_lm_usage()` returns `{model: {prompt_tokens, completion_tokens, total_tokens}}`; default `track_usage=False`. `articles/04-future-use-cases.md:77` [api] (verified behave5.py; `dspy/dsp/utils/settings.py:25`).
- **API-43 cache location and rollouts** — disk cache dir from `DSPY_CACHEDIR` (default `~/.dspy_cache`), size from `DSPY_CACHE_LIMIT` (default 3e10 bytes); `dspy.LM(..., cache=True)` by default; `rollout_id` makes otherwise identical requests distinct cache entries and is stripped before sending, and „rollout_id has no effect when temperature=0; set temperature>0 to bypass the cache." The book skills hand cache hardening to `dspy-production` (`skills/dspy-book-production/SKILL.md:19-21`); only article 02 shows `DSPY_CACHEDIR=.cache/dspy` (`articles/02-how-to-install-and-use.md:114`) [api] (verified `dspy/clients/__init__.py:15-16`, `dspy/clients/lm.py:98-102,168`) → here: `lmrun.py` (`cache=False`), P18.
- **API-44 built-in answer metrics** — `dspy.evaluate.metrics.answer_exact_match(example, pred, trace=None, frac=1.0)` (normalizes; `example.answer` may be a list; `frac<1` → max token-F1 ≥ frac) and `answer_passage_match(example, pred, trace=None)` („does the answer appear anywhere in `pred.context` — a retrieval check, not an answer check"). `skills/dspy-book-metrics/reference.md:28-31` [api] (verified: `"The capital is Paris."` vs `"Paris"` → False; `"The Paris."` → True; German `"Die Kohärenz"` vs `"Kohärenz"` → False, `"Kohaerenz"` ≠ `"Kohärenz"` — only English articles are removed) → here: German corpus; do not rely on `normalize_text` for German surfaces.
- **API-45 dataset loaders (3.3.1)** — `dspy.datasets` exports `Colors, DataLoader, Dataset, HotPotQA, MATH` (and AlfWorld); `GSM8K` lives at `dspy.datasets.gsm8k.GSM8K`. `HotPotQA(*args, only_hard_examples=True, keep_details='dev_titles', unofficial_dev=True, **kwargs)` forwards to `Dataset(train_seed=0, train_size=None, eval_seed=0, dev_size=None, test_size=None, input_keys=None)`; the chapter: `HotPotQA(train_seed=1, train_size=100, eval_seed=2024, dev_size=50, test_size=0)`. `skills/dspy-book-datasets/reference.md:8-12` [api] (verified; constructing HotPotQA downloads data, not done) — see TRAP-2.
- **API-46 names that exist and do not (3.3.1)** — present: `dspy.InferRules`, `dspy.Flex`, `dspy.Retrieve`, `dspy.ColBERTv2`, `dspy.Type`, `dspy.Embeddings`; absent: `dspy.Assert`, `dspy.Suggest` („which `dspy.Refine` superseded"), `dspy.BAMLAdapter`, `dspy.LocalProvider` (module `dspy.clients.lm_local` imports). `skills/dspy-book-coding-agents/reference.md:142-144`, `skills/dspy-book-optimizers/reference.md:140` [api] (verified `hasattr`).
- **API-47 PythonInterpreter permissions** — `PythonInterpreter(deno_command=None, enable_read_paths=None, enable_write_paths=None, enable_env_vars=None, enable_network_access=None, sync_files=True, tools=None, output_fields=None, callbacks=None)`: file system, env and network are closed by default and can be opened per path/var/host through `interpreter_factory`. The skills present „an RLM cannot read host files" as absolute. `skills/dspy-book-modules/SKILL.md:86-89`, `skills/dspy-book-modules/reference.md:68-69` [api] (verified signature) → here: `rlm_ingest.py`.
- **API-48 Deno comes from pip too** — DSPy's interpreter prefers the binary of the optional `deno` PyPI package (`from deno import find_deno_bin`), falling back to `deno` on PATH; the shared venv has `deno 2.9.7` installed this way although `which deno` finds nothing. The skills say „Install Deno from a terminal, not a notebook cell" and article 02 says `brew install deno`. `skills/dspy-book-modules/reference.md:68`, `articles/02-how-to-install-and-use.md:149` [api] (verified `dspy/primitives/python_interpreter.py:84-94`; `importlib.metadata.version('deno')`).

## OPT

- **OPT-1 chapter-6 experimental conditions** — task `DetectAIText` (`text: str` → `is_ai: bool`, docstring „Decide whether the supplied passage was generated by an AI."), 300 rows as 150 semantic human/AI pairs, **pair-grouped** split 160 train / 60 validation / 80 locked test, SHA-256 verified on load; metric `exact_match` accuracy; the test split is „deliberately baseline-adversarial — built from predictions the unoptimized model got wrong"; authors: teaches „optimizer tradeoffs, not a general AI-detector leaderboard". The twelve notebooks are generated templates with no optimizer code — real calls live in `chapter06/optimizer_runtime.py`; notebooks replay checked-in artifacts unless `CHAPTER06_RUN_LIVE=1`. `skills/dspy-book-optimizers/SKILL.md:24-33`, `skills/dspy-book-optimizers/reference.md:6-29` [number][claim].
- **OPT-2 unoptimized baseline** — validation 55.00, test 53.75 (43/80), $0, 0 s. `skills/dspy-book-optimizers/reference.md:29,35` [number] (verified: 43/80 = 53.75, 33/60 = 55.00).
- **OPT-3 LabeledFewShot** — `dspy.LabeledFewShot(k=4).compile(detector, trainset=trainset, sample=True)` → val 63.33, test 67.50 (54/80), +13.75, $0.0000, 0.0 s. 3.3.1: `LabeledFewShot(k=16)` default; `compile(student, *, trainset, sample=True)` works on `student.reset_copy()` and samples with `random.Random(0)` (deterministic: from 8 examples it picks q6, q7, q3, q0; `sample=False` takes the first k). `skills/dspy-book-optimizers/SKILL.md:41,77`, `skills/dspy-book-optimizers/reference.md:36` [number] (verified behave6.py; `dspy/teleprompt/vanilla.py:11-21`).
- **OPT-4 BootstrapFewShot** — `dspy.BootstrapFewShot(metric=exact_match, max_bootstrapped_demos=2, max_labeled_demos=2, max_rounds=1, max_errors=1).compile(detector, trainset=trainset)` → val 66.67, test 67.50 (54/80), +13.75, $0.0030, 4.5 s — „a third of a cent and 4.5 seconds for the same +13.75 as LabeledFewShot". 3.3.1 defaults: `max_bootstrapped_demos=4, max_labeled_demos=16, max_rounds=1, max_errors=None` (→ settings `max_errors=10`). `skills/dspy-book-optimizers/SKILL.md:42,62-63,79-81`, `skills/dspy-book-optimizers/reference.md:37` [number] (verified signature).
- **OPT-5 BootstrapFewShotWithRandomSearch** — `dspy.BootstrapFewShotWithRandomSearch(metric=exact_match, num_candidate_programs=8, max_bootstrapped_demos=2, max_labeled_demos=2, num_threads=1).compile(detector, trainset=trainset, valset=valset)` → val 61.67, test 65.00 (52/80), +11.25, $0.8766, 1119.1 s. 3.3.1 default `num_candidate_programs=16`; compile also takes `restrict=None, labeled_sample=True`. `skills/dspy-book-optimizers/reference.md:38,60-62` [number] (verified signature).
- **OPT-6 KNNFewShot** — `dspy.KNNFewShot(k=4, trainset=trainset, vectorizer=vectorizer, metric=exact_match, max_bootstrapped_demos=0, max_labeled_demos=4).compile(detector)` with „a local hashed n-gram embedder" → val 71.67, test 72.50 (58/80), +18.75, $0.00, 0 s — the best free result. `skills/dspy-book-optimizers/SKILL.md:38,83-84,98-99`, `skills/dspy-book-optimizers/reference.md:39` [number]; how it works on 3.3.1 → OPT-15.
- **OPT-7 COPRO** — `dspy.COPRO(prompt_model=reflection_lm, metric=exact_match, breadth=4, depth=2, init_temperature=1.0, track_stats=True).compile(detector, trainset=valset, eval_kwargs={"num_threads": 1, "display_table": False})` („note: valset passed as trainset") → val 53.33, test 50.00 (40/80), **−3.75**, ≥$0.0732 (lower bound), 861.1 s. 3.3.1 defaults `breadth=10, depth=3, init_temperature=1.4, track_stats=False`; COPRO scores candidates on whatever it receives as `trainset` (`dspy/teleprompt/copro_optimizer.py:138`), so its 53.33 „validation" is its search set, not held out. `skills/dspy-book-optimizers/SKILL.md:47`, `skills/dspy-book-optimizers/reference.md:40,64-67` [number] (verified signature and source).
- **OPT-8 MIPROv2** — `dspy.MIPROv2(metric=exact_match, prompt_model=reflection_lm, task_model=task_lm, auto='light', max_bootstrapped_demos=2, max_labeled_demos=2, seed=42).compile(detector, trainset=trainset, valset=valset, minibatch=True, requires_permission_to_run=False)` → val 76.67, test 66.25 (53/80), +12.50, ≥$0.3052 (lower bound), 270.8 s; the 10.42-point validation-to-test gap is the chapter's overfit example. 3.3.1 defaults: `auto='light'`, `max_bootstrapped_demos=4, max_labeled_demos=4, seed=9`, compile `minibatch=True, minibatch_size=35, minibatch_full_eval_steps=5`. `skills/dspy-book-optimizers/SKILL.md:43,65-67`, `skills/dspy-book-optimizers/reference.md:41,69-72` [number] (verified signature; `requires_permission_to_run` → OPT-21).
- **OPT-9 GEPA** — `dspy.GEPA(metric=feedback_metric, auto='light', reflection_minibatch_size=3, reflection_lm=reflection_lm, num_threads=4, candidate_selection_strategy='pareto', use_merge=False, track_best_outputs=True, track_stats=True, seed=42).compile(detector, trainset=trainset, valset=valset)` → val 80.00, test 80.00 (64/80), **+26.25** (relative +48.8 %), $0.5824 optimization + $0.0396 evaluation, 616.7 s, 684 metric calls, 10 candidates; McNemar p = 0.000104 across 29 discordant pairs; bootstrap 95 % CI [16.25, 36.25] points over 40 pairs and 10,000 samples. „This is a pedagogical optimizer stress test, not evidence that AI-text detection works reliably in the wild." `skills/dspy-book-optimizers/SKILL.md:37,86-90,103-110`, `skills/dspy-book-optimizers/reference.md:42,121-132` [number] (verified: every parameter exists on 3.3.1; relative uplift 26.25/53.75 = 48.8 %; the McNemar p is reproduced exactly by an exact two-sided binomial test on 25 gained vs 4 lost, which is what 43→64 correct with 29 discordant pairs implies — table.py).
- **OPT-10 SIMBA** — `dspy.SIMBA(metric=exact_match, bsize=8, num_candidates=4, max_steps=6, max_demos=2, prompt_model=reflection_lm).compile(detector, trainset=trainset, seed=42)` → val 51.67, test 47.50 (38/80), **−6.25**, **$1.1413 — the most expensive run and the worst result**, 321.3 s. 3.3.1: constructor is keyword-only, defaults `bsize=32, num_candidates=6, max_steps=8, max_demos=4, prompt_model=None, demo_input_field_maxlen=100000, temperature_for_sampling=0.2, temperature_for_candidates=0.2`; `compile(student, *, trainset, seed=0)` — no valset. `skills/dspy-book-optimizers/SKILL.md:48,55-58`, `skills/dspy-book-optimizers/reference.md:43,74-75` [number] (verified signature) → here: `pairs.py` ladder step 4 needs a held-out check before it is trusted.
- **OPT-11 Ensemble** — built from three compiled programs (LabeledFewShot, BootstrapFewShot, BootstrapRS) with `dspy.Ensemble(reduce_fn=boolean_majority).compile([labeled, bootstrapped, searched])`, `boolean_majority = lambda preds: dspy.majority(preds, field="is_ai", normalize=lambda v: str(v).lower())` → val 65.00, test 70.00 (56/80), +16.25, $0.8881, 1151.2 s; evaluation cost 3.7× baseline, mean latency 4.803 s vs 1.789 s (2.68×) „because every component runs on every example, forever". `skills/dspy-book-optimizers/SKILL.md:40,115-121`, `skills/dspy-book-optimizers/reference.md:44,78-93` [number] — mechanics → OPT-23.
- **OPT-12 BootstrapFinetune** — student `Qwen/Qwen2.5-0.5B-Instruct` via `LocalProvider`, teacher the frontier reflection model; PyTorch + Transformers + TRL + PEFT on Apple MPS or CUDA; `use_peft=True`, 10 epochs, batch 1, gradient accumulation 4, learning rate 2e-4, max sequence length 768; the chapter subclasses the optimizer to reject one-class accepted-trace sets („Use a stronger teacher."); accepted traces 77 human / 63 AI; weights git-ignored, only a hash manifest committed → val 70.00, test 70.00 (56/80), +18.75 **from its own 51.25 % baseline**, $0.8651, 1026.3 s. 3.3.1: `BootstrapFinetune(metric=None, multitask=True, train_kwargs=None, adapter=None, exclude_demos=False, num_threads=None)`, `compile(student, trainset, teacher=None)`. `skills/dspy-book-optimizers/SKILL.md:39,50-51`, `skills/dspy-book-optimizers/reference.md:45,52-53,107-117` [number] (verified signature).
- **OPT-13 BetterTogether** — `dspy.BetterTogether(metric=exact_match, p=prompt_optimizer, w=finetuner).compile(detector, trainset=trainset, teacher=teacher, valset=valset, strategy="p -> w", optimizer_compile_args={"p": {"teacher": teacher}})` → val 60.00, test 65.00 (52/80), +13.75 from 51.25, $0.8445, 1740.0 s (the slowest). The override exists „because BetterTogether normalizes its teacher to a list for weight optimizers while BootstrapRS expects one module". 3.3.1: `BetterTogether(metric, **optimizers)` with arbitrary keys (`"mipro -> gepa -> mipro"` works), `strategy="p -> w -> p"` default, `valset_ratio=0.1` holds out 10 % of trainset when no valset, teacher normalized to a list (`dspy/teleprompt/bettertogether.py:315`), result carries `candidate_programs`. `skills/dspy-book-optimizers/SKILL.md:45`, `skills/dspy-book-optimizers/reference.md:46,95-105` [number] (verified signature/source).
- **OPT-14 the four lessons of the table** — (1) optimizing can make it worse: COPRO −3.75, SIMBA −6.25, „If you do not measure a baseline and a held-out test set, you cannot detect this"; (2) free optimizers are not the weak ones; (3) „Validation gain is not test gain" — report the held-out number; (4) „Cost does not predict quality… extra compile spend "buys candidate selection, not a new instruction."" Uplift per dollar among paid runs: BootstrapFewShot 4583 points/$, GEPA 45.1, MIPROv2 41.0, BootstrapFinetune 21.7, Ensemble 18.3, BetterTogether 16.3, BootstrapRS 12.8. `skills/dspy-book-optimizers/SKILL.md:53-72` [number] (verified table.py; the example asserts the same, `skills/dspy-book-optimizers/example_optimizer_results.py:85-116`).
- **OPT-15 KNNFewShot plumbing on 3.3.1** — constructor `KNNFewShot(k, trainset, vectorizer: Embedder, **few_shot_bootstrap_args)` builds the KNN index (embeds the trainset) immediately; `compile(student, *, teacher=None)` only replaces `forward` so that **every inference call** retrieves k neighbours and runs `BootstrapFewShot(**few_shot_bootstrap_args).compile(...)` on them. Hence (a) its $0/0 s „compile" defers the work to inference (with `max_bootstrapped_demos>0` each prediction would bootstrap with extra LM calls — the chapter's `max_bootstrapped_demos=0` avoids that); (b) `num_threads` is not rejected at construction or compile but at the **first forward** (`TypeError: BootstrapFewShot.__init__() got an unexpected keyword argument 'num_threads'`); (c) `save()` writes `demos: []` — the neighbour retrieval is not persisted, a reloaded program is zero-shot. The skills state only „do not pass `num_threads`" and „`trainset`/`vectorizer` in the constructor, only the student in `compile`". `skills/dspy-book-optimizers/SKILL.md:93-96` [trap] (verified behave3.py T11; `dspy/teleprompt/knn_fewshot.py:52-69`).
- **OPT-16 GEPA constructor rules (3.3.1)** — exactly one of `auto`, `max_full_evals`, `max_metric_calls` (AssertionError „Exactly one of max_metric_calls, max_full_evals, auto must be set."); `reflection_lm` or `instruction_proposer` required, asserted at construction („GEPA requires a reflection language model, or custom instruction proposer to be provided. …"; article 02 quotes it as „reflection_lm cannot be None", `articles/02-how-to-install-and-use.md:147`); `track_best_outputs=True` requires `track_stats=True`; `teacher` unsupported; without a valset the trainset is used for both. Defaults: `reflection_minibatch_size=3, candidate_selection_strategy='pareto', skip_perfect_score=True, add_format_failure_as_feedback=False, component_selector='round_robin', use_merge=True, max_merge_invocations=5, failure_score=0.0, perfect_score=1.0, track_stats=False, warn_on_score_mismatch=True, seed=0`. [api] (verified behave4.py T16; `dspy/teleprompt/gepa/gepa.py:427-470,544-545`).
- **OPT-17 GEPA budgets** — `AUTO_RUN_SETTINGS = {light: n=6, medium: n=12, heavy: n=18}` feed `auto_budget(num_preds, num_candidates, valset_size, minibatch_size=35, full_eval_steps=5)`; `max_full_evals` becomes `max_full_evals × (len(trainset) + len(valset))`. Computed metric-call budgets: 1 predictor, 60 val → light 620 / medium 990 / heavy 1455; 1 predictor, 13 val → 432 / 755 / 1126; 2 predictors, 10 val → 790 / 1110 / 1325; 3 predictors, 10 val → 1195 / 1665 / 1960. The chapter's GEPA row (`auto='light'`, one predictor) reports 684 metric calls; if it ran on the 60-row validation split, that is above the 620 the formula gives — an inference, since the „companion experiment" that reports 684 is described as varying dataset size (`skills/dspy-book-optimizers/reference.md:119-129`). Treat `auto` as an estimate, and set `max_metric_calls` when the number matters. [number] (verified behave4.py T16; `dspy/teleprompt/gepa/gepa.py:20-24,490-531,552-559`) → here: `pairs.py` budget planning.
- **OPT-18 GEPA's feedback handling** — a float from the per-predictor call becomes `feedback = "This trajectory got a score of {score}."`, which is why „With a scalar-only metric it has nothing to reflect on and you pay the reflection model anyway"; a per-predictor score that differs from the module-level score is ignored with the warning „Currently, GEPA does not support predictor level scoring … GEPA will ignore the differing score returned, and instead use module level score" — so per-predictor feedback text is used, per-predictor scores are not. `skills/dspy-book-optimizers/SKILL.md:112-113` [api] (verified `dspy/teleprompt/gepa/gepa.py:584-605`, `dspy/teleprompt/gepa/gepa_utils.py:423-432`).
- **OPT-19 `skip_perfect_score` explains two null results** — `dspy.GEPA(skip_perfect_score=True)` (default) skips reflection on all-correct minibatches: the invoice benchmark's GEPA made 393 task requests and **zero** reflection requests and returned the original instruction (`skills/dspy-book-use-cases/reference.md:51-55`); article 03's RAG example „Iteration 1 produced an instruction that scored perfect on the full valset. GEPA spent the rest of the budget correctly refusing to mutate." (`articles/03-inside-the-examples.md:48`); the math example with GLM 4.5 Air (baseline 83.33 %) and Ministral 8B (93.33 %) accepted zero mutations (`articles/03-inside-the-examples.md:75`). [number] (verified the default).
- **OPT-20 GEPA as batch inference-time search** — `valset=trainset, track_stats=True, track_best_outputs=True`, then `optimized.detailed_results.best_outputs_valset` holds the best output per task; article 04 proposes this „Pareto inference-time selection" as not yet used. `articles/04-future-use-cases.md:75` [api] (verified docstring `dspy/teleprompt/gepa/gepa.py:235-236`).
- **OPT-21 MIPROv2 `requires_permission_to_run`** — deprecated: `False` logs „'requires_permission_to_run' is deprecated and will be removed in a future version."; `True` raises `ValueError: User confirmation is removed from MIPROv2. Please remove the 'requires_permission_to_run' argument.` The chapter's call passes `False` — works with a warning. `skills/dspy-book-optimizers/reference.md:72` [api] (verified `dspy/teleprompt/mipro_optimizer_v2.py:128-136`).
- **OPT-22 COPRO swallows unknown arguments** — `COPRO.__init__(…, **_kwargs)` silently ignores any misspelt or unsupported keyword. [trap] (verified signature/source).
- **OPT-23 Ensemble mechanics (legacy teleprompter)** — `Ensemble(*, reduce_fn=None, size=None, deterministic=False)`; `compile(programs)` takes a program list, no trainset, no metric; the returned program runs the programs **sequentially** in a list comprehension (latency adds up), `size` draws a random subset per call with the global `random`, `deterministic=True` fails an assertion („TODO: Implement example hashing for deterministic ensemble."), no `reduce_fn` returns the list of outputs. `skills/dspy-book-optimizers/SKILL.md:115-121`, `skills/dspy-book-optimizers/reference.md:142` [api] (verified `dspy/teleprompt/ensemble.py:10-40`).
- **OPT-24 recompiling does not stack demos in 3.3.1** — optimizers compile a `student.reset_copy()`: three successive `LabeledFewShot(k=4)` compiles leave 4 demos and the original untouched; `BootstrapFewShot.compile` on an already-compiled program raises `AssertionError: Student must be uncompiled.` The skills' „compiling the same object three times stacks demos — `deepcopy()` first" is false on 3.3.1 (`deepcopy()` is harmless). `skills/dspy-book-optimizers/reference.md:91-92`, `skills/dspy-book-datasets/reference.md:137-138` [trap] (verified behave6.py; `dspy/teleprompt/bootstrap.py:96-102`).
- **OPT-25 cost bookkeeping of chapter 6** — COPRO and MIPROv2 costs are lower bounds because „those runs predate a fix to shared LM history accounting"; total recorded cost across all smoke and full runs „at least $7.60"; the eleven listed full-run costs sum to $5.58 and their wall times to 7111 s. `skills/dspy-book-optimizers/reference.md:48-55` [number] (verified the sums).
- **OPT-26 why GEPA won, per the chapter** — its metric returned a diagnosis, not a score, and the instruction told the model *what to judge* (writing style, not topic or technical sophistication); „GEPA earns its cost when failures can be described in words." `skills/dspy-book-optimizers/SKILL.md:101-113` [claim].
- **OPT-27 the judge-first GEPA run (chapter 3)** — `judge_optimizer = dspy.GEPA(metric=exact_match, max_full_evals=3, num_threads=4, track_stats=True, use_merge=False, reflection_lm=smart_lm)`; `optimized_judge = judge_optimizer.compile(AIDetector(), trainset=trainset, valset=valset)`; `optimized_judge.save("./ai_detector/", save_program=True)`; `loaded = dspy.load("./ai_detector/")` (fails on 3.3.1, TRAP-3); then a closure rebinds the task metric to the optimized judge and a second GEPA run optimizes the transformer. `skills/dspy-book-eight-steps/reference.md:50-61` [recipe] (verified parameters).
- **OPT-28 GEPA on a real multi-signature program (chapter 11 image-CLI)** — `dspy.GEPA(metric=cli_metric, reflection_lm=..., max_metric_calls=600, reflection_minibatch_size=8, candidate_selection_strategy="pareto", num_threads=6, track_stats=True, log_dir="./gepa_logs").compile(student=ImagePopulator(...), trainset=trainset, valset=valset)`; three signatures = three instruction slots; data ~10 real failures + 50 synthesized, split 50 train / 10 val („expansion is justified there because CLI rollouts are cheap and the metric scores generated images, so no gold images are needed"); optimize against the same cheap models you ship, then `optimized.save(...)` and `production.load_state(optimized.dump_state())`. `skills/dspy-book-coding-agents/reference.md:92-111` [recipe][number] (verified parameters; load_state caveat TRAP-4).
- **OPT-29 selection rules the slice gives** — start with LabeledFewShot/KNNFewShot/BootstrapFewShot („Start here, always"), escalate only when failures can be described in words (GEPA), never GEPA with a scalar metric, never trust validation, never treat Ensemble's gain as free, never quote chapter-6 numbers as a general ranking. `skills/dspy-book-optimizers/SKILL.md:60-63,123-131` [pattern].
- **OPT-30 the saturation lesson (articles)** — GEPA needs failures in the minibatches: grade-school math with GLM 4.5 Air (32 B, baseline 83.33 %) and Ministral 8B (93.33 %) accepted zero mutations; Liquid LFM 2.5 (1.2 B) went 45.00 → 70.00 over 23 iterations with mutations accepted at iterations 2, 3, 4, 6, 10 (candidate 4 won), ~23 min, `num_threads=1`, free tier, reflection Nemotron-3-super-120B-a12b — a DSPy 3.1.3 run. „if you run the pack and see baseline == optimized, check the difficulty of your data relative to your task LM". Gemma 3 4B on OpenRouter „rejects system prompts outright". `articles/03-inside-the-examples.md:75-81,114-122` [number][claim] (verified against `examples/02-math-reasoning/version_comparison.md`: the 45→70 row is labelled „Historical, 3.1.3"; the current committed run is different, TRAP-21).
- **OPT-31 chapter-level configuration of the judge optimizer** — `dspy.BootstrapFewShot(metric=judge_metric, max_bootstrapped_demos=8, max_labeled_demos=8)` compiled on `judge_trainset[:split]` with `split = max(1, int(len(judge_trainset) * 0.8))`, student `dspy.ChainOfThought(JudgeQuality)`. `skills/dspy-book-metrics/reference.md:105-113` [recipe] (verified parameters).

## MET

- **MET-1 the chapter-5 ladder** — each rung added because the previous broke: „exact match is too strict, regex is all-or-nothing, all-or-nothing is brittle, edit distance ignores meaning, embeddings need a threshold, subjective tasks need humans, one judge has blind spots, a rubric still hides *which step* failed." `skills/dspy-book-metrics/SKILL.md:25-29` [pattern].
- **MET-2 metric by output shape** (the skill's synthesis; „The chapter never states this table"): one canonical short answer → `answer_exact_match` („The capital is Paris." fails against „Paris"); required structure, free wording → regex (binary, give partial credit); set of required points → partial-credit keyword overlap (word boundaries or `car` matches `carbon`); OCR/spelling → Levenshtein with a threshold (`good`/`great` is 3 edits, `good`/`goof` is 1); paraphrase of a known answer → embedding cosine (blind to negation); extractive span → token F1 (rewards overlap, not correctness); translation/summary → BLEU/ROUGE (weaker than embeddings); subjective quality → judge calibrated on human labels (untrusted below 80 %); several axes → weighted rubric (check both extremes); multi-step pipeline → trace-aware per-predictor feedback (latency on every call). `skills/dspy-book-metrics/SKILL.md:31-46`, `skills/dspy-book-metrics/example_metric_recipes.py:24-35` [recipe] (verified the EM row, API-44).
- **MET-3 binary metrics give no gradient** — „All-or-nothing metrics make optimizers brittle. A small change can flip the score from 0 to 1 with no smooth gradient in between." Brittle: `all(k in pred.answer.lower() for k in example.keywords)`; usable: `matches / len(example.keywords)`. The example's check: „A metric an optimizer can climb takes more than two distinct values" (`len(set(scores)) > 2`). `skills/dspy-book-metrics/SKILL.md:48-62`, `skills/dspy-book-metrics/example_metric_recipes.py:93-95` [recipe] (verified dry run: partial `[0.0, 0.25, 0.5, 0.75, 1.0]` vs binary `[0,0,0,0,1]` as the answer improves).
- **MET-4 the chapter reintroduces the problem** — `semantic_similarity_metric` binarizes cosine at `0.8` „without flagging that it just reintroduced the problem. Return the similarity." `skills/dspy-book-metrics/SKILL.md:60-62` [recipe].
- **MET-5 keyword partial credit** — `pattern = r'\b' + re.escape(keyword.lower()) + r'\b'`; `return matches / len(example.keywords)`; guard empty keywords by returning 1.0. `skills/dspy-book-metrics/reference.md:40-45`, `skills/dspy-book-metrics/example_metric_recipes.py:45-52` [recipe] (verified dry run).
- **MET-6 regex structure metric** — `header_pattern = r'(?m)^##\s+.+'`; `len(re.findall(header_pattern, pred.answer)) >= 3`. `skills/dspy-book-metrics/reference.md:33-38` [recipe].
- **MET-7 edit distance** — `from Levenshtein import distance`, pass if `dist <= threshold` (default 3); OCR and spelling only. `skills/dspy-book-metrics/reference.md:47-48` [recipe].
- **MET-8 embedding similarity** — `dspy.Embedder("openai/text-embedding-3-small")`, cosine = `np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))`; cosine runs −1..1, „above roughly 0.8 indicates a strong semantic match; a paraphrase lands near 0.85, unrelated text near 0.45"; `multi_field_semantic_metric` averages over several fields skipping empties and returns `np.float64` („cast before JSON logging"). Caching helper keyed on `hashlib.md5(text.encode()).hexdigest()` into a pickled dict — „The chapter defines this helper but never wires it into the metrics". `skills/dspy-book-metrics/reference.md:50-68` [recipe][number].
- **MET-9 BLEU, ROUGE, token F1** — `sentence_bleu([example.answer.split()], pred.answer.split()) >= 0.5` (nltk); `Rouge().get_scores(pred.answer, example.answer)[0]['rouge-l']['f'] >= 0.5` using the `rouge` package, not `rouge-score`, instantiated inside the metric in the notebook („Hoist it"); `token_f1` is set-overlap precision/recall on lowercased whitespace tokens with empty guards; BLEU's brevity penalty is the chapter's only anti-gaming note. Verdict: „These traditional NLP metrics tend to underperform semantic similarity for most DSPy tasks, but they're nearly free to run and well-validated in the academic literature — worth keeping in your toolkit, especially when you need a fast sanity check during optimization." `skills/dspy-book-metrics/reference.md:70-92` [recipe] (verified the example's token F1: „the treaty was signed" vs „…not signed" = 0.89 — overlap metrics score a reversed fact high).
- **MET-10 return types in chapter 5** — `bool`: `exact_match, blog_structure_metric, keyword_inclusion_metric v1, edit_distance_metric, semantic_similarity_metric, bleu_metric, rouge_metric, judge_metric`; `float`: `keyword_inclusion_metric v2, token_f1, marketing_rubric_metric, weighted_marketing_metric`; `np.float64`: `multi_field_semantic_metric`; `dspy.Prediction(score, feedback)`: `llm_judge_metric, multi_hop_metric, pipeline_metric_with_feedback`. „Only three are GEPA-compatible. The rest use the three-argument form". `skills/dspy-book-metrics/reference.md:11-19` [claim].
- **MET-11 judge calibration protocol** — „The chapter's most valuable sequence, and the only one with a stated trust bar": (1) label by hand with a human metric that prints the case, blocks on input and always asks why („'Why' is more valuable than 'Yes/No' for LLMs."), run with `num_threads=1`, append each label to JSONL so labelling and trainset construction are one pass; (2) 20–50 labels, more if subtle; (3) judge Signature `is_good: bool`, `reasoning: str`; (4) optimize the judge on 80 % with plain label equality; (5) measure on the held-out 20 %: „If agreement is in the 80–90% range, the judge is trustworthy enough to use as the metric for optimizing your real program. Below that, collect more labels or refine the judge signature."; (6) promote it to a GEPA metric with its reasoning as feedback. „Do not skip step 5. An uncalibrated judge is an unfalsifiable metric". `skills/dspy-book-metrics/SKILL.md:64-84`, `skills/dspy-book-metrics/reference.md:94-119` [recipe] → here: any model judge for job 1/2 (P27).
- **MET-12 the judge signature** — `JudgeQuality`: docstring „Evaluate whether a model's answer is accurate and helpful."; inputs `question`, `answer`, `gold_answer` (desc „The correct answer for reference"); outputs `is_good: bool` („True if answer is accurate and helpful"), `reasoning: str` („Explain your judgment"); `judge_metric(example, pred, trace=None): return pred.is_good == example.is_good` („plain agreement, no kappa"). `skills/dspy-book-metrics/reference.md:96-106` [recipe].
- **MET-13 agreement is not chance-corrected** — „80% on a balanced binary task is closer to 60% corrected — treat the chapter's 80–90% bar as a floor, not a ceiling." `skills/dspy-book-metrics/reference.md:121-123` [number] (verified: Cohen's κ = (0.8−0.5)/(1−0.5) = 0.6). The example's `judge_verdict` is stricter than the chapter: ≥0.90 „trustworthy", 0.80–0.90 „usable, but re-measure after optimizing against it", below „not trustworthy" (`skills/dspy-book-metrics/example_metric_recipes.py:19-20,74-80`).
- **MET-14 promoting the judge** — `def llm_judge_metric(example, pred, trace=None, pred_name=None, pred_trace=None): result = optimized_judge(question=example.question, answer=pred.answer, gold_answer=example.answer); return dspy.Prediction(score=1.0 if result.is_good else 0.0, feedback=result.reasoning)`. `skills/dspy-book-metrics/SKILL.md:75-81` [recipe] (verified GEPA reads `.score`/`.feedback`, OPT-18).
- **MET-15 weighted rubric** — one Signature (`EvaluateMarketing`: `copy` in; `clarity, persuasiveness, brand_fit, emotional_appeal: int` each „1-5: …", `reasoning`), weights `{"clarity": 0.3, "persuasiveness": 0.4, "brand_fit": 0.2, "emotional_appeal": 0.1}`, `normalized = (weighted_sum - 1) / 4.0`; weights must sum to 1.0; „score your known-best and known-worst examples and confirm they land where you expect". Both notebooks build `dspy.ChainOfThought(...)` inside the metric body — hoist it. `skills/dspy-book-metrics/SKILL.md:86-100`, `skills/dspy-book-metrics/reference.md:125-141` [recipe] (verified the example: best 1.00, worst 0.00, mixed 0.55; weights not summing to 1 raise).
- **MET-16 trace-aware per-predictor feedback** — `pipeline_metric_with_feedback(example, pred, trace=None, pred_name=None, pred_trace=None)`: `if trace is None and pred_name is None: return is_correct`, else `_, inputs, outputs = pred_trace[-1]` and branch on `pred_name` (`"generate_query"`: „query too short, entities lost"; `"synthesize"`), returning `dspy.Prediction(score=score, feedback=feedback)`. „Skip them for single-predictor programs or when evaluation latency matters more than feedback quality — trace analysis adds overhead on every call." Trace elements are `(predictor object, inputs dict, Prediction)` — „the first element is the predictor **object**, not a name string". `skills/dspy-book-metrics/SKILL.md:102-120`, `skills/dspy-book-metrics/reference.md:143-163` [recipe] (verified the trace tuple shape, behave3.py T17; the branch predicate breaks under BootstrapFewShot → TRAP-16).
- **MET-17 what chapter 5 leaves out** — judge gaming (re-measure agreement after optimizing, not only before), position and length bias, devset overfitting from repeated optimization against one set, cosine blind to negation („The treaty was signed" / „…not signed"). „They are this skill's additions, not the book's". `skills/dspy-book-metrics/SKILL.md:122-129`, `skills/dspy-book-metrics/reference.md:165-173` [claim] → here: P27, `baseline.py`.
- **MET-18 deterministic pattern metric beats a judge when failures are enumerable** — the quickstart replaces the judge with ~18 compiled regex patterns for known tells (em dashes, curly quotes, overused words), strict (any hit scores 0), feedback naming the matched patterns and hit count; dataset ten or so hand-written pairs, each commented with its tell; final steps probe one sentence per tell. „It costs nothing, never drifts, and the feedback is more specific than a judge's." „Reach for a judge when the quality you want cannot be written as a pattern — not before." `skills/dspy-book-eight-steps/SKILL.md:76-84`, `skills/dspy-book-eight-steps/reference.md:80-90` [recipe] → here: P1, `fold()` first.
- **MET-19 the task metric as judge-as-metric (chapter 3)** — run the detector on the transformed text and reward fooling it; feedback assembled from the verdict, the judge's reasoning, a similar labelled example and its notes. `skills/dspy-book-eight-steps/reference.md:46-48` [recipe].
- **MET-20 deterministic gate before the judge (financial analyst)** — `value = parse_financial_number(pred.answer)` (normalizes `$1.25B`, `-2.8%`, `€42 million`; raises on unparseable, caught as failure); `if not math.isclose(value, gold, rel_tol=0.05): return 0.0` — judge never called; else the judge grades the trajectory for context poisoning and drift: `0.3 if verdict.poisoning_detected else 0.7 if verdict.drift_detected else 1.0`. „Cheap check first, expensive check second, and partial credit that encodes *why*." It works in plain evaluation, where the optimizer-only trace argument is absent. `skills/dspy-book-use-cases/SKILL.md:65-77`, `skills/dspy-book-use-cases/reference.md:65-80`, `skills/dspy-book-use-cases/example_use_case_router.py:61-90` [recipe] (verified dry run: wrong and unparseable answers make zero judge calls; percent parsing → TRAP-19).
- **MET-21 two metrics, separated (invoice extraction)** — the optimizer sees a partial-credit metric `((0.5 * present) + (0.5 * correct)) / 8` with the five-argument signature; a strict exact-match fraction is used for reporting only, never given to the optimizer. `skills/dspy-book-use-cases/reference.md:28-31` [recipe].
- **MET-22 efficiency metric for agent cost** — score trajectories by non-terminal tool calls, „full credit for two or fewer tool calls, less beyond", and hand it to an optimizer: „Optimize the agent to be cheap rather than hard-coding a cap." The example's values (1.0 at ≤2, 0.8 at ≤4, 0.5 beyond) are the example's, not quoted from the chapter. `skills/dspy-book-agents/SKILL.md:96-104`, `skills/dspy-book-agents/reference.md:97-100`, `skills/dspy-book-agents/example_agent_budget.py:17-18,37-45` [recipe].
- **MET-23 binary verdict judge that raises** — test-agents-md judge sees the antipattern and the tell and returns `VERDICT: <PASS|FAIL>`; „carried over from running tests to checking conventions — binary, no Likert scale to drift on"; an unparseable verdict raises „so a broken judge cannot quietly look like a failing candidate". Example regex `r"VERDICT:\s*(PASS|FAIL)"`, case-insensitive. `skills/dspy-book-coding-agents/SKILL.md:91-94`, `skills/dspy-book-coding-agents/reference.md:66-71`, `skills/dspy-book-coding-agents/example_artifact_optimizer.py:25,82-87` [recipe] → here: P23.
- **MET-24 mechanical + judge composite (landing page)** — `score = 0.5 * mechanical + 0.25 * visual_win + 0.25 * voice_win`; `mechanical` = mean of five binary checks with no model (renders under Playwright; every brand hex colour in the CSS; brand font referenced; no horizontal overflow at 375 px; section count matches the brief); „noise-free signal is worth more per unit than judge signal." Both judges are pairwise against gold HTML with randomized position: `swap = random.random() < 0.5`; `candidate_won = (verdict == ("A" if not swap else "B"))`. `skills/dspy-book-coding-agents/SKILL.md:96-114`, `skills/dspy-book-coding-agents/reference.md:46-64` [recipe].
- **MET-25 per-rule rubric judge (persona)** — clawsona judge emits PASS/FAIL per rule of an eight-rule rubric, then `SCORE: <0-1>` parsed with a regex; unparseable raises. `skills/dspy-book-coding-agents/reference.md:79-90` [recipe].
- **MET-26 visual per-slot judge (image CLI)** — the metric averages the module's own per-slot visual judge scores and threads the failure mode into the feedback string. `skills/dspy-book-coding-agents/reference.md:103-104` [recipe].
- **MET-27 parse failures stay in the denominator** — chapter 6 keeps parse failures as incorrect with `status: parse_error`, „never dropped from a denominator. Copy that discipline." `skills/dspy-book-optimizers/reference.md:134-135` [pattern] → here: P15, `lmrun.py` status `unparsed`.
- **MET-28 statistics for one optimizer claim** — McNemar test on the paired correct/incorrect outcomes plus a pair-grouped bootstrap CI (10,000 samples over 40 pairs) — „the chapter's one statistically defended result". `skills/dspy-book-optimizers/SKILL.md:103-110` [pattern] (verified the p-value arithmetic, OPT-9).
- **MET-29 the examples' feedback metrics (articles)** — RAG QA: correctness 0.55 (fuzzy substring or token overlap), citation validity 0.30 (at least one cited id in gold, extras penalized), conciseness 0.15 (3–25 words); invoice: schema validity 0.20, vendor 0.15, date 0.15, line-item F1 0.35 over `(fuzzy_description, quantity, unit_price)` triples, total 0.15; math: each item carries a `trap` field (e.g. „200*1.25=250, 250*0.8=200. The sequence matters; do NOT add the percentages.") fed into feedback so reflection learns structural fixes, not answers. A metric that returns 0.7 „tells the reflection model nothing"; one that returns „wrong answer; cited doc ['mars'] but the evidence is in ['jupiter']" „teaches it everything". `articles/03-inside-the-examples.md:40-46,65-73,100`, `articles/01-why-dspy-agent-skills.md:34` [recipe].

## DATA

- **DATA-1 conversion fixes the types** — one `convert` per source; `.with_inputs()` names only what the program receives, every other field is gold; coerce and map labels at conversion, never in the metric. HuggingFace two inputs: `dspy.Example(question=ex['question'], context=ex['context'], answer=ex['answers']['text'][0]).with_inputs('question', 'context')`; label map `{0: "negative", 1: "neutral", 2: "positive"}`; CSV `is_disaster=bool(row['target'])`; pandas `[dspy.Example(**ex).with_inputs("text") for ex in df.to_dict(orient='records')]`. „Getting `with_inputs` wrong is the quiet failure: a field left out is unavailable to the program, and a gold field left in leaks the answer." `skills/dspy-book-datasets/SKILL.md:24-45`, `skills/dspy-book-datasets/reference.md:6-20` [recipe] (see TRAP-15 for a misspelt name).
- **DATA-2 seeded disjoint splits** — `random.Random(2024).shuffle(all_examples)` („seeded literal, not random.seed()"); 50/25/25 for a hand-built set; for a large public corpus 200 train („deterministically sampled"), 100 validation („held out while the optimizer searches"), 500 test („final evaluation only"), validation and test from the same upstream split but **disjoint index ranges** — „Verify that yourself; nothing enforces it." `skills/dspy-book-datasets/SKILL.md:73-83`, `skills/dspy-book-datasets/reference.md:22-42` [recipe] (verified the example: same seed reproduces, seed+1 differs, a 2-row overlap is caught).
- **DATA-3 chapter 3 splits with the global seed** — `random.seed(42); random.shuffle(dataset); trainset, valset = dataset[:len(dataset)//2], dataset[len(dataset)//2:]` — 50/50, no test split, the form chapter 4 advises against. `skills/dspy-book-eight-steps/reference.md:22-28` [recipe].
- **DATA-4 difficulty tiers** — three named tiers, a few examples each, so „each pattern from the error analysis is represented": clear (baseline should pass), boundary (might fail), ambiguous (hard for any model). Ambiguous cases are not fixed: they go to a separate log with `needs_clarification`, `default_routing` and `reasoning`, because „they tell you where the task definition itself is incomplete" — a specification finding, not a model failure. `skills/dspy-book-datasets/SKILL.md:47-61`, `skills/dspy-book-datasets/reference.md:44-53` [pattern] → here: an ambiguous surface pair is a decision file, not a trainset row.
- **DATA-5 error notes are the feedback channel** — annotate each error with prediction, gold and the mechanism: `{"ticket": ..., "pred_department": ..., "gold_department": ..., "notes": "Model sees 'can't find' and assumes technical issue, but this is an account management question."}`; chapter 3's CSV carries a `notes` column that becomes the GEPA feedback string — „the dataset ships the diagnosis, not just the label". `skills/dspy-book-datasets/SKILL.md:63-71`, `skills/dspy-book-datasets/reference.md:55-75`, `skills/dspy-book-eight-steps/reference.md:30-31` [pattern] → here: the `rule` field of the surface-pair trainset is this channel.
- **DATA-6 synthetic generation** — `FactGeneration` (docstring „Generate facts and their veracity."; `sindex: str` „a random string seed" → `fact: str`, `veracity: bool`) with `dspy.Predict(FactGeneration, n=15)`; `examples = [dspy.Example(fact=f, answer=v).with_inputs('fact') for f, v in zip(response.completions.fact, response.completions.veracity)]`. Diversity comes only from `sindex` and `temperature=1`; „There is no quality filter, no validation pass and no anti-collapse check in the chapter." Synthetic data from the model being optimized carries its biases into its own training set. Add a dedup pass, a quality gate (a metric, or a second model disagreeing), and a length assertion because „`n>1` is unsupported on several reasoning endpoints and `zip` truncates silently". `skills/dspy-book-datasets/SKILL.md:85-107`, `skills/dspy-book-datasets/reference.md:77-97` [recipe][claim] (verified the example's `synthetic_batch_is_sound`: 8 of 15 returned and 11 unique of 15 both fail).
- **DATA-7 enrichment instead of generation** — when real outputs exist but the inputs are missing, write the inverse signature (`TellJoke`: topic → joke; `IdentifyTopic`: joke → topic) and synthesize only the inputs: „A real joke with a synthesized topic is far better training data than a synthesized joke" — „the chapter's strongest guard against training on a model's own distribution". `skills/dspy-book-datasets/SKILL.md:109-112`, `skills/dspy-book-datasets/reference.md:99-110` [pattern].
- **DATA-8 PII synthesis** — `SynthesizeTicket` („Generate a synthetic support ticket with similar structure but fake details."; `original_ticket` → `synthetic_ticket` „Synthetic ticket with fake names, emails, account numbers"), gold label carried over unchanged. Unstated caveats: the real PII still reaches the provider; nothing verifies the output dropped the identifiers; nothing checks the model did not echo a real name — add a diff or regex check. `skills/dspy-book-datasets/SKILL.md:114-122`, `skills/dspy-book-datasets/reference.md:112-123` [pattern] → here: any corpus text sent to a provider needs the author's yes.
- **DATA-9 leakage rules the book does not state** — no warning about leakage, contamination or devset overfitting anywhere in chapter 4; chapter 3 evaluates on the full dataset including train; one notebook optimizes against `testset[:20]`. Supply: validation and test never overlap each other or train; the test set is touched once, at the end; repeated optimization against one devset stops measuring quality. „do not attribute leakage rules to the book". `skills/dspy-book-datasets/SKILL.md:124-133`, `skills/dspy-book-datasets/reference.md:125-130` [claim].
- **DATA-10 chapter-6 data** — 300 rows = 150 human/AI pairs; split by pair so „a passage and its rewrite never straddle the split"; 160/60/80; SHA-256 verified on load. `skills/dspy-book-optimizers/reference.md:23-25` [pattern] → here: split surface pairs by term family, not by row.
- **DATA-11 invoice benchmark data** — five hand-written invoices, each text plus eight gold fields, three train and two holdout; one case with genuinely empty gold fields to test that the model emits empty strings rather than inventing values; evaluation repeated three times over the holdout (six predictions per approach). `skills/dspy-book-use-cases/reference.md:22-26,36-42` [number].
- **DATA-12 artifact-optimization datasets** — 5 to 20 hand-captured real failures („You are encoding what actually goes wrong"); the AGENTS.md benchmark is 20 cases of `task` / `antipattern` / `tell`; the persona set is 10 deliberately heterogeneous prompts (debugging question, production page, Slack draft, technology choice, farewell post, outage apology) so Pareto keeps register-specific strengths; landing-page cases carry `brief, brand_guide, expected, failure_mode, notes, gold_html`; image CLI ~10 real + 50 synthesized, 50/10. `skills/dspy-book-coding-agents/SKILL.md:55,68-75`, `skills/dspy-book-coding-agents/reference.md:62-64,68,79-86,105-107` [number] → here: job 4.
- **DATA-13 the examples' sizes (articles)** — RAG: 12 short solar-system articles, 25 questions split 15/10, BM25 top-3; math: 34 grade-school word problems (committed run: 34 train / 12 val); invoice: 20 train / 9 val with deliberate traps (DD-MM-YYYY, „March 8, 2024" and „22 September 2024" dates; seller vs bill-to/shipper; discount/rebate rows that are not line items; freight/handling lines; tax-inclusive vs pre-tax totals). „These are single-seed runs on small train/val splits (15–34 train, 9–12 val)". `articles/03-inside-the-examples.md:22,28,63,102-108` [number] (verified sizes against `examples/*/results.json`).
- **DATA-14 the example stand-in's uid** — `example_dataset_builder.py` identifies an example by `str(sorted(self.fields.items()))`, so two identical rows in different splits are flagged as leakage by content, not by id. `skills/dspy-book-datasets/example_dataset_builder.py:44-45,63-70` [pattern].
- **DATA-15 DummyVectorizer reseeds the global RNG** — `dspy.utils.dummies.DummyVectorizer()` calls `random.seed(123)` in its constructor; building one (e.g. as a free KNN vectorizer, like the chapter's hashed n-gram embedder) resets any global-seed split. [trap] (verified behave3.py T11; `dspy/utils/dummies.py:191`) → here: use `random.Random(seed)` instances only.

## RLM

- **RLM-1 when to use RLM** — „a sandboxed REPL with your context as variables, plus a cheap sub-model" for when „the context is too long to reason over directly, roughly 50,000 tokens and up"; ProgramOfThought when execution beats reasoning (arithmetic, counting, sorting, data transforms); CodeAct when the work needs your functions composed in code. `skills/dspy-book-modules/SKILL.md:74-80`, `skills/dspy-book-modules/example_module_choice.py:16,29-39` [claim] → here: measure `rlm_ingest.py` documents against the threshold before assuming RLM.
- **RLM-2 sandbox** — all three run on Deno plus Pyodide, file system, env vars and network restricted by default, „which is why an RLM cannot read host files and you must pass contents as input values"; „RLM is marked experimental in DSPy and is the most likely of these to change." `skills/dspy-book-modules/SKILL.md:86-89`, `skills/dspy-book-modules/reference.md:68-74` [api] (permissions are configurable, API-47; Deno source, API-48).
- **RLM-3 RLM limits (3.3.1)** — `max_iters=20` bounds REPL steps; `max_llm_calls=50` bounds sub-LLM calls (`llm_query`/`llm_query_batched`) per execution and raises „LLM call limit exceeded" when exceeded; `max_output_chars=10000` caps REPL output shown to the model; the agents chapter calls `max_llm_calls` „an explicit call ceiling distinct from the iteration count". `skills/dspy-book-agents/SKILL.md:102`, `skills/dspy-book-agents/reference.md:95` [api] (verified `dspy/predict/rlm.py:143-172,269-271`) → here: `rlm_ingest.py` call budget.
- **RLM-4 skill discovery with RLM** — `class DiscoverSkill(dspy.Signature)`: „Read every session transcript in `session_texts` … Group similar summaries. For the largest group, write a SKILL.md with YAML frontmatter (name, description) and a body of concrete rules drawn from how the assistant actually responded. Skip groups smaller than 2 sessions. Return only the SKILL.md text."; `session_texts: list[str]` → `skill_md: str`; `dspy.RLM(DiscoverSkill, max_iters=8, max_llm_calls=10, sub_lm=...)`; the host reads the transcripts and passes texts; output goes to disk for human review before install. `skills/dspy-book-coding-agents/SKILL.md:132-139`, `skills/dspy-book-coding-agents/reference.md:113-129` [recipe].
- **RLM-5 RLM inside a GEPA-optimized module (article 04)** — `RepoAuditor` with `self.explore = dspy.RLM("repo_tree, question -> findings", max_iters=30, sub_lm=dspy.LM("openai/gpt-4o-mini"))` and `self.synthesize = dspy.ChainOfThought("findings, question -> report")`, compiled with `dspy.GEPA`. The article says GEPA gets „two instruction surfaces"; on 3.3.1 it gets three: `explore.generate_action`, `explore.extract`, `synthesize.predict`. `articles/04-future-use-cases.md:26-48` [api] (verified behave4.py T13).
- **RLM-6 the financial analyst** — `ProgramOfThought` plus `RLM`, judged separately, graded with MET-20. `skills/dspy-book-use-cases/reference.md:16` [pattern].
- **RLM-7 paper claims (articles)** — RLMs „separate *variable space* … from *token space*"; an RLM on Qwen3-8B „outperforms the base Qwen3-8B by 28.3% on average and approaches GPT-5 on long-context benchmarks", processing inputs „two orders of magnitude beyond the underlying context window". `articles/01-why-dspy-agent-skills.md:40-42` [claim].

## RAG

- **RAG-1 the retriever interface** — any callable returning an object with `.passages`, „which is why the same RAG module works unchanged over an in-memory index and over an external vector database. Swapping the backend changes one line." `skills/dspy-book-agents/SKILL.md:74-77` [pattern] (verified `dspy.retrievers.Embeddings` returns `passages` and `indices`, API-36) → here: `graphrag.py` returns quotations; the seam is the same shape.
- **RAG-2 external vector store warning** — „the collection's vector dimensions, vector name, embedding model and document field must all match the client configuration, or retrieval silently returns nothing useful"; the external path uses a third-party package and an older RM-style class, not core DSPy. `skills/dspy-book-agents/SKILL.md:79-81`, `skills/dspy-book-agents/reference.md:67-70` [trap][claim].
- **RAG-3 do not copy MultiHopQA** — `dspy.Retrieve(k=3)` with `dspy.ColBERTv2(url="http://20.102.90.50:2017/wiki17_abstracts")`, „the legacy retrieval path pointed at a public endpoint that is frequently unreachable". `skills/dspy-book-metrics/SKILL.md:137`, `skills/dspy-book-metrics/reference.md:175-180` [trap] (the classes still exist in 3.3.1, API-46).
- **RAG-4 two ways to end multi-hop** — agent-controlled `dspy.ReAct("question -> answer, sources: list[str]", tools=[...], max_iters=12)` (finish tool or `max_iters`, cost varies) vs a fixed-depth module looping `for _ in range(self.num_hops)` (default 3, no early exit) with three predictors `question, notes -> query`, `question, notes, context -> new_notes: list[str]`, `question, notes -> answer`, deduplicating notes and sources with a membership check; the fixed version exists „For a predictable retrieval budget." `skills/dspy-book-agents/SKILL.md:83-94`, `skills/dspy-book-agents/reference.md:72-85` [pattern].
- **RAG-5 dedupe between hops** — „Deduplicate notes and sources between hops, or hop two re-retrieves hop one." Example: `list(dict.fromkeys(existing + new))`. `skills/dspy-book-agents/SKILL.md:94`, `skills/dspy-book-agents/example_agent_budget.py:95-97` [pattern].
- **RAG-6 research fan-out** — news researcher: decompose → `Parallel` × `ReAct` → synthesize „preserving conflicts"; it has no metric at all. `skills/dspy-book-use-cases/SKILL.md:29`, `skills/dspy-book-use-cases/reference.md:13` [pattern] → here: never-merge (P13) has a precedent here.
- **RAG-7 doc compaction** — `chapter11/assets/compacted-dspy-docs.md`: the whole DSPy docs site flattened into one ~163 KB file, each chunk titled with its full breadcrumb as H1 and repeated as H2 so an isolated chunk still carries its path; no notebook loads it; it still documents `dspy.Assert`/`dspy.Suggest` — „If you hand a compacted doc to an agent as ground truth, date it and re-verify it." `skills/dspy-book-coding-agents/reference.md:131-144` [pattern][number].
- **RAG-8 citations as an output field (article 03)** — `AnswerWithCitation`: `context`, `question` → `answer` („short, factual answer — no prose padding"), `citations: list[str]` („doc IDs from the context that support the answer"); GEPA's gain was teaching the synthesizer to emit citations even when the model „knew" the answer — „a format problem, not a knowledge problem". `articles/03-inside-the-examples.md:28-38,59` [pattern].

## AGENT

- **AGENT-1 MCP lifecycle** — „DSPy does not manage the connection: you own the client lifecycle, and the tools are only valid inside the session's context manager": `stdio_client(StdioServerParameters(command=sys.executable, args=[server_script]))` → `ClientSession(read, write)` → `await session.initialize()` → `await session.list_tools()` → `[dspy.Tool.from_mcp_tool(session, t) for t in mcp_tools.tools]` → `dspy.ReAct("user_request -> response", tools=tools, max_iters=10)` → `await agent.acall(user_request=...)`. The chapter ships its own ~20-line `FastMCP` server with `@mcp.tool()` and `mcp.run(transport="stdio")` — no port, no second terminal. Extra installs: `mcp`, `langchain-core`. `skills/dspy-book-agents/SKILL.md:22-43`, `skills/dspy-book-agents/reference.md:6-31` [recipe] (API verified by source, API-32).
- **AGENT-2 the async rule, as the chapter states it** — „A synchronous `agent(...)` call with an asynchronous converted tool raises `ValueError` by default. Prefer `await agent.acall(...)`; enable `dspy.configure(allow_tool_async_sync_conversion=True)` only when you deliberately want DSPy to bridge the two execution modes." `skills/dspy-book-agents/reference.md:26-29` [claim] — false for ReAct on 3.3.1, see TRAP-11.
- **AGENT-3 memory in four steps** — (1) `dspy.History` is a field type, not a store; (2) summarize overflow: keep the last N turns verbatim, compress the rest into a synthetic first turn, with the summarizer instruction „Summarize older turns without inventing information. Preserve current goals, constraints, unresolved issues, and corrections. When details conflict, keep the newest version." (signature `previous_summary, turns: list[dict[str, str]] -> summary`); (3) store `react_result.answer` in history, never `react_result.trajectory`, „Otherwise every large tool payload is replayed on every later call"; (4) Mem0 `Memory.from_config(config)` (LLM + embedder blocks) exposed as three plain ReAct tools around `memory.add`, `memory.search(filters=...)`, `memory.get_all(filters=...)`, each returning `response.get("results", [])`, wrapped in try/except with a readiness flag. `skills/dspy-book-agents/SKILL.md:45-70`, `skills/dspy-book-agents/reference.md:33-55` [recipe].
- **AGENT-4 `max_iters` sizing** — „5 for a narrow check, 15 for open research"; the chapter uses 10 (MCP agent) and 12 (multi-hop); the example interpolates `{"narrow": 5, "moderate": 8, "broad": 12, "open": 15}`. `skills/dspy-book-agents/SKILL.md:101`, `skills/dspy-book-agents/reference.md:94`, `skills/dspy-book-agents/example_agent_budget.py:19` [number].
- **AGENT-5 no reliability layer in chapter 8** — „no retry logic, no timeouts except one, and no cost caps"; error style is „graceful degradation — check the key, return a message string, set a readiness flag, never raise". `skills/dspy-book-agents/SKILL.md:96-107`, `skills/dspy-book-agents/reference.md:87-103` [claim] → here: a returned message string is indistinguishable from an answer unless it is recorded as a status (P15).
- **AGENT-6 ReAct subagents** — a subagent is a plain function that calls another ReAct and returns a string; the manager records one tool call, the worker keeps its own trajectory. The chapter's `calculate` tool uses `eval` and says so — never expose it to untrusted input. `skills/dspy-book-agents/reference.md:115-119` [pattern].
- **AGENT-7 reading a trajectory** — iterate an index range and break on the first missing `tool_name_{i}`; count non-`finish` tool names for efficiency. `skills/dspy-book-agents/reference.md:111-113`, `skills/dspy-book-agents/example_agent_budget.py:37-67` [recipe] (verified the key format matches ReAct 3.3.1, API-35).
- **AGENT-8 framework comparison has no conclusion** — the same two-tool agent in DSPy, LangChain, Pydantic AI, the OpenAI Agents SDK and CrewAI, one task, one model; „It contains no conclusion. There is no verdict cell, no summary table, no winner." Its only editorial statements: the model is held constant, and the async runner is used because the notebook already owns an event loop. `skills/dspy-book-agents/SKILL.md:109-118`, `skills/dspy-book-agents/reference.md:121-127` [claim].
- **AGENT-9 chapter-8 cost and services** — every notebook needs an LLM key; the external vector store needs a running service; web search needs a search-API key; the memory notebook installs `mem0ai` and makes its own model and embedder calls; chapter 8 „costs roughly one to three dollars end to end". `skills/dspy-book-agents/reference.md:129-134` [number][claim].
- **AGENT-10 sandwich guardrail for a tool-using agent** — cheap compiled-regex check → LLM injection classifier (`is_injection: bool` plus a reason) → only if both pass, the ReAct agent with deliberately narrow tools (document search, order lookup) → deterministic PII redaction of the output; a detected injection returns a fixed refusal that „never reaches the tools". „the cheap check runs first so most attacks cost nothing". `skills/dspy-book-use-cases/SKILL.md:79-87`, `skills/dspy-book-use-cases/reference.md:82-90`, `skills/dspy-book-use-cases/example_use_case_router.py:93-99` [pattern].

## PROD

- **PROD-1 load once, serve async** — FastAPI `lifespan`: `dspy.configure(lm=dspy.LM(MODEL), async_max_workers=4)`; `app.state.extractor = dspy.asyncify(load_invoice_extractor())`; handler `result = await app.state.extractor(text=request.text); return result.toDict()`; never load or compile per request. `skills/dspy-book-production/SKILL.md:23-39`, `skills/dspy-book-production/reference.md:6-24` [recipe] (verified `asyncify`, `toDict`, API-37/38; FastAPI not run).
- **PROD-2 worker sizing** — `async_max_workers` „is sized from the provider's rate limit, not the CPU count. Start at 4 and raise under load test while watching for 429s." (DSPy default 8.) The example's own formula is Little's law: `in_flight = (rpm / 60) * mean_latency_s` → 60 rpm × 2 s ≈ 2, 600 rpm × 2 s ≈ 20, 3000 rpm × 1.5 s ≈ 75. `skills/dspy-book-production/SKILL.md:41-42`, `skills/dspy-book-production/example_serving_checks.py:34-39` [recipe] (verified dry run).
- **PROD-3 missing artifact must stop the service** — the chapter's loader calls `.load(path)` only when the artifact exists and otherwise prints a warning, and itself says to replace that with a hard startup failure; „a service silently serving an uncompiled program is worse than one that refuses to boot". `skills/dspy-book-production/SKILL.md:44-47`, `skills/dspy-book-production/reference.md:26-28` [trap].
- **PROD-4 guardrails as optimizable fields** — declare the check as an output field, e.g. `is_invoice: bool = dspy.OutputField(desc="False if the text is not an invoice")`, so the optimizer improves it with the task; the chapter cites a DSPy guardrails result where attack success fell from 75 % to 5 %; three layers: Pydantic `field_validator` bounding the input (e.g. 50,000 characters), the optimizable field, a deterministic output regex returning 422; wrap in `dspy.Refine(module=..., N=3, reward_fn=passes_guardrail, threshold=1.0)` „so a guardrail miss retries rather than fails outright". `skills/dspy-book-production/SKILL.md:49-64`, `skills/dspy-book-production/reference.md:33-42` [recipe][claim] — Refine returns the best failing output after N misses, TRAP-13.
- **PROD-5 typed fallback only** — `except (APIError, RateLimitError, ServiceUnavailableError, Timeout): with dspy.context(lm=fallback_lm): ...`; „A bare catch turns your own bugs into silent fallbacks to a weaker model." The chapter's own handler still wraps everything in `except Exception as exc: raise HTTPException(500, str(exc))` (returns the exception text to the client). `skills/dspy-book-production/SKILL.md:66-75`, `skills/dspy-book-production/reference.md:19-23,44-53`, `skills/dspy-book-production/example_serving_checks.py:20-21,42-48` [recipe] → here: `lmrun.py` status `unreachable` must come only from typed provider errors.
- **PROD-6 export the prompt, adapter first** — `dspy.settings.lm.history[-1]["messages"]` is the rendered prompt; „if a human will read it, configure a readable adapter **before** optimizing. Swapping adapters at export time renders demos that were selected under one format in a different one." `skills/dspy-book-production/SKILL.md:77-84` [trap].
- **PROD-7 MLflow tracing** — `mlflow server --backend-store-uri sqlite:///mydb.sqlite` („SQLite rather than the file store, which degrades past a few hundred runs"); `mlflow.set_tracking_uri("http://127.0.0.1:5000")`, `mlflow.set_experiment("DSPy")`, `mlflow.dspy.autolog(log_compiles=True, log_evals=True, log_traces_from_compile=True)`; a span tree per call (module → adapter format → raw request/response → adapter parse → output); a `compile()` creates a parent run plus one child run per evaluation holding candidate program state, score and traces; manual model comparison = one run per model logging score, token count, cost and latency percentiles; no model registry used. Pins `mlflow==3.15.1`. `skills/dspy-book-production/SKILL.md:86-109`, `skills/dspy-book-production/reference.md:62-85` [claim] (mlflow not installed).
- **PROD-8 MLflow MCP server** — `mlflow[mcp]` exposes traces to an agent, „26 tools, 11 of them trace tools"; the two that matter: search traces for triage, and log **feedback** („a judgment about what happened") or an **expectation** („ground truth, curatable into a later trainset"); „always pass an explicit field list when searching traces, using dot paths, or the default full span tree exhausts the context window after a few results" — „the single most useful operational detail in the chapter"; scope tools with the server's tool-filter environment variable. The example uses `("info.trace_id", "info.state", "data.spans.*.name")` as a field list. `skills/dspy-book-production/SKILL.md:99-106`, `skills/dspy-book-production/reference.md:87-97`, `skills/dspy-book-production/example_serving_checks.py:18,64-69` [claim].
- **PROD-9 Gradio UI pattern** — two tabs: runtime signature construction with `make_signature({name: (str, dspy.InputField(desc=...))}, instructions=...)`, module dropdown, CSV upload split 80/20 into examples, optimizer and metric dropdowns, state-only save; an inference tab showing the prediction **and** the rendered prompt. `skills/dspy-book-production/reference.md:99-105` [pattern].
- **PROD-10 a benchmark harness with usage accounting** — token accounting by snapshotting LM history before and after each phase; evaluation repeated three times over the holdout; one result document with generated-at, status, model names, train and holdout ids, per-approach usage, learned instructions, and an explicit limitations list. `skills/dspy-book-use-cases/SKILL.md:41-45`, `skills/dspy-book-use-cases/reference.md:36-40` [pattern] → here: `Plan/runs/<subject>/lm/` and `baseline.py` already do this.
- **PROD-11 invoice benchmark results** (2026-08-09, six predictions per approach): baseline exact 81.25 % / partial 90.62 % / $0.0012; labelled few-shot **87.50 %** / 90.62 % / $0.0015; per-field majority voting over three completions 81.25 % / 90.62 % / $0.0027; GEPA 81.25 % / 90.62 % / $0.0013 (393 task requests, 0 reflection requests, instruction unchanged). „The run did not reproduce the book's illustrative table"; the only gain came from few-shot, „from one repeat correctly emitting empty strings for genuinely absent fields". `skills/dspy-book-use-cases/SKILL.md:57-63`, `skills/dspy-book-use-cases/reference.md:42-55` [number] (checked: the percentages equal 39, 42 and 43.5 of 48 field scores — 8 fields × 6 predictions).
- **PROD-12 free-tier operating notes (articles)** — the examples use `num_threads=1` and `num_retries=12` against OpenRouter's free tier („20 requests/minute and 2000/day"); paid fallback `openrouter/mistralai/ministral-3b-2512` at „$0.10/M tokens"; env overrides `DSPY_TASK_MODEL`, `DSPY_REFLECTION_MODEL`, `DSPY_CACHEDIR`; the invoice run „exhausted OpenRouter's 2000 req/day free cap right before the final re-eval". `articles/02-how-to-install-and-use.md:107-117,145`, `articles/03-inside-the-examples.md:112` [claim].
- **PROD-13 loading saved per-predictor LMs** — see TRAP-4 (`load_state` strips `api_base`/`base_url`/`model_list`). [api]

## TEST

- **TEST-1 the dry-run convention** — every example has `--dry-run`, deterministic assertions, no LM, no network, no key; run as `uv run python example_*.py --dry-run`. All nine pass on the shared DSPy 3.3.1 interpreter. [pattern] (verified: nine × `dry run OK`, exit 0) → here: P5.
- **TEST-2 the dry-runs cannot detect DSPy drift** — none of the nine examples imports `dspy`; each checks the skill's own stand-ins (`Prediction`, `Example`, routing tables). They pass unchanged while `from dspy.datasets import GSM8K`, `dspy.load(dir)`, `dspy.BAMLAdapter`, `dspy.Image(path)` and the MCP sync-call claim fail or behave differently on 3.3.1. `skills/dspy-book-eight-steps/example_eight_steps.py:67-73` („Stand-in for dspy.Prediction so this example needs no dspy import"), `skills/dspy-book-datasets/example_dataset_builder.py:6` [trap] (verified) → here: `check_dspy_surface.py` is the kind of check that catches it.
- **TEST-3 a committed benchmark artifact is the regression test** — the invoice test is plain `unittest`, static, never calls an API: (1) every code cell parses, execution counts cleared, no outputs committed; (2) the holdout is exactly as declared, train and holdout disjoint, each approach has exactly six predictions drawn only from holdout ids — „That is a leakage guard."; (3) the four recorded scores still match, reflection requests are still zero, and the learned instruction still equals the original. „commit the result artifact, then assert against the artifact". `skills/dspy-book-use-cases/SKILL.md:39-55`, `skills/dspy-book-use-cases/reference.md:57-63` [recipe] → here: `baseline.py`, `judgements.py` replay.
- **TEST-4 inspect-based guards** — `metric_is_optimizer_ready`: ≥5 parameters; `best_of_n_reward_shape_ok`: exactly 2; `evaluator_signature_ok`: second parameter named `example`. `skills/dspy-book-eight-steps/example_eight_steps.py:54-60,87-93`, `skills/dspy-book-coding-agents/example_artifact_optimizer.py:61-69` [recipe] (verified they classify the documented cases; `evaluator_signature_ok` flags a working `(candidate, **kw)` evaluator as wrong — a false alarm, SKILL-4).
- **TEST-5 step-order checker** — the eight steps with prerequisites (`signatures`; `modules`←signatures; `explore`←modules; `dataset`←signatures; `metrics`←dataset; `baseline`←metrics, modules; `optimize`←baseline; `iterate`←optimize); `violations({"signatures","modules","dataset","metrics","optimize"}) == ["optimize done without baseline"]`; CLI `--next signatures,modules,dataset`. `skills/dspy-book-eight-steps/example_eight_steps.py:19-51,96-158` [pattern] (verified dry run).
- **TEST-6 smoke budget first** — every chapter-11 notebook ships a `max_metric_calls=3` smoke cell beside the full-budget cell; „the smoke run catches the `example` trap and the objective's format constraint before you spend a budget". `skills/dspy-book-coding-agents/reference.md:40-42` [recipe].
- **TEST-7 DSPy's own offline LM** — `dspy.utils.dummies.DummyLM(answers, follow_examples=False, reasoning=False, adapter=None)`: a list of dicts (served in order, then `{"answer": "No more responses"}`), a dict keyed by a substring of the last message, or `follow_examples=True` (answers from matching demos); it honours `n` (so `config={"n": 5}` yields five completions) and reports zero usage. `DummyVectorizer` (hashed char n-grams) is a free embedder for KNN tests. [api] (verified in every behaviour probe; `dspy/utils/dummies.py:16,141,184`) → here: `lm_fixture.py` can reuse the answer-script shape.
- **TEST-8 a full offline `dspy.GEPA` run** — `dspy.GEPA(metric=..., max_metric_calls=30, reflection_lm=DummyLM([{"new_instruction": "..."}]*50), num_threads=1, track_stats=True, seed=0).compile(dspy.Predict(Sig), trainset=..., valset=...)` with a DummyLM task LM completes offline in under a second and exercises the real metric call shapes. [recipe] (verified gepa1.py; `detailed_results.val_aggregate_scores` populated) → here: `pairs.py` ladder fixture.
- **TEST-9 a full offline `optimize_anything` run** — pass a plain Python callable `prompt -> str` as `ReflectionConfig(reflection_lm=...)`; it is wrapped in `TrackingLM` and never touches litellm. [recipe] (verified oa1.py: 12 metric calls, 1 reflection call, `best_candidate` improved) → here: job 4 fixture.
- **TEST-10 the dataset checks** — seeded split reproduces; a new seed gives a different split; an injected 2-row overlap is caught; tier coverage counted; unknown tier raises. `skills/dspy-book-datasets/example_dataset_builder.py:102-152` [pattern] (verified dry run).
- **TEST-11 test counts in the articles are stale** — „The final test suite has 60 assertions" / „Sixty automated tests" describe upstream v0.1.0. `articles/01-why-dspy-agent-skills.md:67`, `articles/04-future-use-cases.md:18` [claim]; the test suite rules they name: „no dict metrics, no `.overall_score`, every skill has a runnable example with `--dry-run`" (`articles/04-future-use-cases.md:96`).

## PAT

- **PAT-1 the eight steps, in order** — 1 specify signatures (the task and, if needed, its judge), 2 build modules (`Predict`/`ChainOfThought`), 3 explore a few examples by hand (ending in `inspect_history`), 4 collect the dataset (`dspy.Example`, seeded split), 5 define metrics (one per thing measured, five-argument form), 6 establish a baseline („a number to beat, before any compile"), 7 optimize (judge first, then task), 8 test and iterate (best-of-N, a different provider). „Steps 3 and 6 are the ones people skip. Step 3 costs a few calls and tells you whether the signature is even close. Step 6 is what makes step 7 falsifiable." `skills/dspy-book-eight-steps/SKILL.md:21-33`, `skills/dspy-book-eight-steps/reference.md:6-18` [pattern] → here: CLAUDE.md's „by hand first".
- **PAT-2 optimize the judge before the task** — „When the metric is itself a model, it is a program, and an unoptimized program is a bad metric." „optimizing a task against an unvalidated judge moves the program toward the judge's errors, and you cannot tell from the score that it happened." Chapter 3 compiles the judge on labelled data, saves, reloads, then optimizes the task against it. `skills/dspy-book-eight-steps/SKILL.md:35-43` [pattern] → here: P27.
- **PAT-3 step 8 checks** — wrap the optimized program in best-of-N: „If it does [help], the program is under-determined"; re-run evaluation with `optimized_transformer.set_lm(dspy.LM(<other provider>))`: „A program that only works on the model it was optimized against is overfitted to that model". `skills/dspy-book-eight-steps/SKILL.md:65-70`, `skills/dspy-book-eight-steps/reference.md:77-78` [pattern].
- **PAT-4 metric-to-reward bridge** — the chapter adapts its judge metric to BestOfN by rebuilding an Example from the reward kwargs: `dummy = dspy.Example(**kwargs).with_inputs(*kwargs.keys()); return float(optimized_llm_judge(dummy, pred))`. The rebuilt Example carries inputs only, so this works for a judge that needs no gold field and fails for one that does. `skills/dspy-book-eight-steps/reference.md:65-72` [pattern] (verified the kwargs shape, API-6).
- **PAT-5 eight-steps anti-patterns** — optimizing before a baseline; optimizing against a judge never checked against human labels; a three-argument metric that must be rewritten for GEPA; skipping step 3 then debugging an optimizer; evaluating on rows the optimizer trained on; assuming transfer to another model. `skills/dspy-book-eight-steps/SKILL.md:96-103` [pattern].
- **PAT-6 seven applications by task shape** — one label, fuzzy boundary → sentiment classifier (inline signature + few-shot random search; „the **demos** carry the label definition, not the prompt"); many fields from text → invoice extraction (partial credit, per-field voting, committed benchmark); private data with tools → customer service (narrow tools in a sandwich); one question needing many lookups → news researcher (decompose, parallel fan-out, synthesize preserving conflicts); numbers over long documents → financial analyst (code execution + trajectory-graded metric); text with no gold → blog writer (embedding distance to reference passages); expensive irreversible call → video generator (quality gate before the step). Routing rule: gold answer → classification/extraction with accuracy or partial credit plus an optimizer; no gold → judge or embedding reward with `Refine`; lookup → retrieval; arithmetic → code execution with a deterministic parse in the metric. `skills/dspy-book-use-cases/SKILL.md:22-37`, `skills/dspy-book-use-cases/reference.md:6-20` [pattern].
- **PAT-7 gate before the expensive step** — video generator: `Refine(module=ImageGenerator(), N=3, reward_fn=image_reward, threshold=0.8)` with a vision judge scoring the image (score/10); only an image that clears the bar reaches the video call; both external APIs are stubbed so the notebook runs free. `skills/dspy-book-use-cases/SKILL.md:32,100`, `skills/dspy-book-use-cases/reference.md:92-100` [pattern].
- **PAT-8 grade the trajectory** — „A right answer from bad reasoning is a latent failure." `skills/dspy-book-use-cases/SKILL.md:74-77` [pattern] (recipe MET-20).
- **PAT-9 ship honest failures** — the invoice benchmark „did not reproduce the book's illustrative table" and says so in the repository: „Shipping that finding, in the repository, is the practice to copy." `skills/dspy-book-use-cases/SKILL.md:57-63` [pattern] → here: `baseline.py` records null results.
- **PAT-10 composition without error handling** — „The chapter contains no stage-level error handling at all: no try/except, no fallback stage, no validation gate between stages. The only failure mechanisms anywhere are `BestOfN`/`Refine` thresholds and `Parallel(max_errors=)`." „No module is flagged as rarely worth using; the tour is uniformly positive." `skills/dspy-book-modules/SKILL.md:121-124`, `skills/dspy-book-modules/reference.md:111-117` [claim].
- **PAT-11 module and adapter routing (example)** — context ≥ 50,000 tokens → RLM; execution + tools → CodeAct; execution only → ProgramOfThought; tools only → ReAct; else ChainOfThought/Predict. Adapter: reasoning model → TwoStepAdapter; Pydantic output → JSONAdapter; readable transcript → XMLAdapter; else ChatAdapter. `skills/dspy-book-modules/example_module_choice.py:19-53` [pattern].
- **PAT-12 chapter-9 gaps** — „No Pydantic models anywhere" (primitives and generics only); „No human-in-the-loop step" in any of the seven (the closest is hand-labelling a small set to confirm the judge); the blog writer defines a metric it never optimizes with and the news researcher has no metric; two notebooks use unannotated signatures; one declares a numeric score as a string and parses it with `float()`; one declares `rationale` alongside ChainOfThought. `skills/dspy-book-use-cases/SKILL.md:89-102`, `skills/dspy-book-use-cases/reference.md:18-20,102-108` [claim].
- **PAT-13 the pack's premise (article 01)** — agents „silently use a deprecated import or return a dict from its metric in a way that crashes `dspy.Evaluate`"; skills fit because of progressive disclosure — Willison: skills are „deliciously tiny": metadata at startup, SKILL.md on activation, references on demand. `articles/01-why-dspy-agent-skills.md:14,51` [claim].
- **PAT-14 paper claims quoted by the articles** — DSPy (Khattab et al., ICLR 2024): pipelines „outperform standard few-shot prompting (generally by over 25% and 65%) and pipelines with expert-created demonstrations (by up to 5–46% and 16–40%)" on GPT-3.5 and Llama2-13B; GEPA (Agrawal et al., ICLR 2026 Oral): beats GRPO-style RL „by 6% on average and up to 20%, while using up to 35× fewer rollouts", beats MIPROv2 „by over 10%" on benchmarks like AIME-2025; GEPA = „Genetic-Pareto" („Genetic-Evolutionary Prompt Adaptation" appears in no primary source). `articles/01-why-dspy-agent-skills.md:26,32,36`, `articles/README.md:18-29` [claim].
- **PAT-15 ideas the articles list as not built** — RLM inside a GEPA-optimized module (waiting for a real use case); GEPA-optimizing the reflection LM itself on harvested (failure, successful-mutation) pairs, explicitly „research speculation — NOT implemented"; serving with a `/healthz` that runs a synthetic prediction; a pre-deploy pytest that reads a prior `results.json` and fails CI below tolerance; Pareto inference-time selection; `track_usage` logging; a fine-tuning skill; benchmark parity with the GEPA paper (AIME-2025, BIG-Bench Hard, HotpotQA, IFEval). `articles/04-future-use-cases.md:22-91` [claim].

## SKILL

- **SKILL-1 which tool for which artifact** — a markdown file loaded as a system prompt → `gepa.optimize_anything` („there is no program to compile"); a DSPy program with signatures → `dspy.GEPA(...).compile(...)` („each predictor gets its own instruction slot"); the image-CLI notebook is the counter-example that correctly uses `dspy.GEPA`, the other four notebooks correctly use `optimize_anything`. „The chapter is one move repeated five times. Learn the move." `skills/dspy-book-coding-agents/SKILL.md:24,61-64`, `skills/dspy-book-coding-agents/reference.md:6-15` [pattern] → here: job 4.
- **SKILL-2 the call** — `from gepa.optimize_anything import optimize_anything, GEPAConfig, EngineConfig, ReflectionConfig`; `optimize_anything(seed_candidate=open("SKILL.md").read(), evaluator=evaluator, dataset=CASES, objective=..., config=GEPAConfig(engine=EngineConfig(max_metric_calls=80), reflection=ReflectionConfig(reflection_lm=REFLECTION_MODEL)))`; `result.best_candidate` is the new file text (a `str` when the seed is a `str`, a dict when the seed is a dict of named parameters). The evaluator in the skill calls the task LM directly: `TASK_LM(messages=[{"role": "system", "content": candidate}, {"role": "user", "content": example["prompt"]}])[0]` — an LM called with `messages=` returns a list of strings. `skills/dspy-book-coding-agents/SKILL.md:26-51`, `skills/dspy-book-coding-agents/reference.md:17-29` [recipe] (verified: all four names import from gepa 0.1.4; full offline runs, TEST-9; LM call returns a list).
- **SKILL-3 gepa version** — the book pins `gepa==0.1.1`; DSPy 3.3.1 declares `gepa[dspy]==0.1.4` as a dependency, so the shared venv already has **gepa 0.1.4**, where `optimize_anything` exists — no extra install, and no way to get 0.1.1 beside DSPy 3.3.1 without breaking its pin. Everything below is verified against 0.1.4; 0.1.1 may differ. `skills/dspy-book-coding-agents/reference.md:3-4,160-162` [api] (verified `importlib.metadata.version('gepa')`).
- **SKILL-4 evaluator contract (0.1.4)** — the candidate is passed positionally (its parameter name is free); the dataset item is injected **by keyword `example`** and then filtered against the evaluator's parameter names (`accepted_params = set(sig.parameters.keys())`, unless it has `**kwargs`, which receives everything); a parameter named `opt_state` receives an `OptimizationState` with historical best evaluations; the return is `(score, side_info)` or a bare `score`; in single-task mode (`dataset=None, valset=None`) `example` is not passed at all. The example's `evaluator_signature_ok` checks only that the second parameter is literally named `example`, so a working `(candidate, **kw)` evaluator is flagged as wrong. `skills/dspy-book-coding-agents/SKILL.md:59`, `skills/dspy-book-coding-agents/reference.md:31-36`, `skills/dspy-book-coding-agents/example_artifact_optimizer.py:61-69` [api] (verified oa3.py; `gepa/optimize_anything.py:393-450,995-1021`).
- **SKILL-5 the renamed-parameter trap, three ways** — `evaluator(candidate, task)`: with the default `raise_on_exception=True` the run dies with `TypeError: renamed() missing 1 required positional argument: 'task'` raised 12 frames deep in `wrapped_evaluator` (what the skill describes: „GEPA silently drops the data, then crashes several layers deep"); with `EngineConfig(raise_on_exception=False)` the run **finishes** — every evaluation becomes `(0.0, {"error": ...})`, val score `[0.0]`, the seed is returned; `evaluator(candidate, item=None)` also finishes silently, every call receiving `item=None` (val score `[0.5]`, seed returned). `skills/dspy-book-coding-agents/SKILL.md:59,151` [trap] (verified oa3.py; `gepa/optimize_anything.py:1044,1057-1066`) → here: P23 — assert the name before the first call, never set `raise_on_exception=False`.
- **SKILL-6 side_info is the reflector's window** — „The reflection LM sees only the score and that dict. Always include the judge's reasoning, or it is optimizing blind." In 0.1.4 each example's side_info is rendered under `## Evaluation Results` as `# Example n` with one `## <Key>` heading per key; `"log"`, `"stdout"`, `"stderr"` are reserved for GEPA's own capture (`oa.log()`, `EngineConfig(capture_stdio=True)`), a colliding key is stored under `_gepa_<key>` with a warning; a `"scores": {name: float}` dict in side_info feeds a per-objective Pareto frontier; side_info may carry `gepa.Image` objects, which turn the reflection prompt multimodal. `skills/dspy-book-coding-agents/SKILL.md:56`, `skills/dspy-book-coding-agents/example_artifact_optimizer.py:103-106` [api] (verified the rendered reflection prompt in oa1.py; `gepa/optimize_anything.py:1068-1106`, `gepa/adapters/optimize_anything_adapter/optimize_anything_adapter.py:675`).
- **SKILL-7 objective and background** — `objective` is rendered verbatim under `## Optimization Goal`, above `## Current Component` (the candidate inside ``` fences), `## Evaluation Results`, `## Your Task` and `## Output Format`; `background` adds domain knowledge; `seed_candidate=None` („seedless mode") requires `objective` and a reflection LM; `objective`/`background` cannot be combined with a custom `reflection_prompt_template`. „Every notebook closes with "Output ONLY the <file> text. No preamble, no markdown fences."" `skills/dspy-book-coding-agents/SKILL.md:57`, `skills/dspy-book-coding-agents/reference.md:37` [api] (verified oa1.py prompt; `gepa/optimize_anything.py:520-610,1271,1585`) — but see SKILL-8.
- **SKILL-8 the output format fights gepa's template** — gepa 0.1.4's own template ends „Provide ONLY the improved version within ``` blocks", and the proposal is extracted as the text between the **first and the last** ``` of the reflection output (a first line without spaces after the opening fence is dropped as a language tag). If the reflection model obeys the book's „no markdown fences" on a file that itself contains a code block, the extracted candidate is only the text between the file's own first and last fence: a 90-character SKILL.md with frontmatter and one ```bash block came back as `'python x.py'` (11 characters). Fenced output, fenced output with a language tag, and a preamble before the fence all extract correctly. `skills/dspy-book-coding-agents/SKILL.md:46,57` [trap] (verified oa2.py; `gepa/strategies/instruction_proposal.py:125-153`, `gepa/optimize_anything.py:608-610`) → here: job 4 — ask for the file inside one ``` block, and check the frontmatter mechanically in the evaluator.
- **SKILL-9 three modes, and the book uses the one without a held-out set** — `dataset=None, valset=None`: single-task search (one candidate is the solution); `dataset` only: multi-task search, `valset` defaults to `dataset`; `dataset` + `valset`: generalization. The book's recipe passes `dataset=CASES` only, so candidate selection and reflection run on the same cases — nothing measures transfer to unseen cases, which is why its regressions (SKILL-14) are expected. `skills/dspy-book-coding-agents/SKILL.md:42-49` [api] (verified `gepa/optimize_anything.py:1282,1368`; fixture_oa.py passes `valset`) → here: job 4 needs a held-out set of failures.
- **SKILL-10 EngineConfig defaults (0.1.4)** — `run_dir=None, seed=0, display_progress_bar=False, raise_on_exception=True, use_cloudpickle=True, track_best_outputs=True, max_metric_calls=None, max_candidate_proposals=None, max_reflection_cost=None, val_evaluation_policy="full_eval", candidate_selection_strategy="pareto"` (also `current_best`, `epsilon_greedy`, `top_k_pareto`), `frontier_type="hybrid"`, `acceptance_criterion="strict_improvement"` (or `improvement_or_equal`), **`parallel=True`, `max_workers=os.cpu_count() or 32`**, `cache_evaluation=False, cache_evaluation_storage="auto"` (disk when `run_dir` is set, else memory), `best_example_evals_k=30, capture_stdio=False`. The skill names `max_metric_calls`, `run_dir`, `track_best_outputs`, `cache_evaluation` — all exist. `skills/dspy-book-coding-agents/reference.md:38` [api] (verified oa3.py and `gepa/optimize_anything.py:452-517`).
- **SKILL-11 the evaluator runs concurrently** — with the default `parallel=True`, `optimize_anything` called the evaluator from 2 threads in a 12-call run on a 4-CPU container; an evaluator that calls a rate-limited free tier or mutates shared state needs `parallel=False` or its own lock. [trap] (verified oa1.py thread ids).
- **SKILL-12 ReflectionConfig defaults (0.1.4)** — `reflection_lm="openai/gpt-5.1"`, converted with litellm when it is a string (so a forgotten `reflection_lm` is a live, paid call to OpenAI); a Python callable `prompt -> str` is accepted and wrapped in `TrackingLM`; `reflection_lm_kwargs` forwarded to litellm; `reflection_minibatch_size` defaults to 1 in single-task mode and 3 otherwise; `skip_perfect_score=False` (unlike `dspy.GEPA`, OPT-19); `module_selector="round_robin"` (or `"all"`); `batch_sampler="epoch_shuffled"`; `custom_candidate_proposer`, `reflection_strategy` for advanced use. `MergeConfig` (`max_merge_invocations=5, merge_val_overlap_floor=5`) and `RefinerConfig` (`refiner_lm` defaults to the reflection LM, `max_refinements=1`, auto-injects a `refiner_prompt` parameter into the candidate) are both off unless set. [trap][api] (verified `gepa/optimize_anything.py:717-800,1286,1388-1394`) → here: P5 — an offline fixture must pass a callable reflection LM, never rely on the default.
- **SKILL-13 stopping and budget accounting** — at least one stopper is required (`ValueError: At least one stopping condition must be provided via config.engine.max_metric_calls or config.stop_callbacks.`); `run_dir` adds a file stopper on `<run_dir>/gepa.stop` and a `run_log.txt`; `max_candidate_proposals` and `max_reflection_cost` are further stoppers. The budget is checked between steps: 3 cases, `max_metric_calls=12` used exactly 12 with one reflection call (consistent with seed evaluation, reflection minibatch, new-candidate minibatch and new-candidate evaluation, 3 calls each); 2 cases with a separate valset and `max_metric_calls=8` used **10**. [api][number] (verified oa1.py, fixture_oa_count.py; `gepa/optimize_anything.py:1400-1436`).
- **SKILL-14 read the regressions** — `regressed = [c['antipattern'] for c, b, o in zip(CASES, baseline_scores, optimized_scores) if b > o]`; „GEPA drops rules that did not fire during the evaluation. That is correct behaviour for the benchmark and wrong for your repository, because your file also encodes rules no case exercised." Measure the baseline first, re-score every case after, print the regression list, hand-restore what was discarded, diff against the current file; keep the old artifact — „the artifact is the only way to diagnose regressions." The example shows a net +1.0 hiding one regression. `skills/dspy-book-coding-agents/SKILL.md:116-130`, `skills/dspy-book-coding-agents/example_artifact_optimizer.py:97-100,142-148` [recipe] (verified dry run) → here: job 4 must diff against the current SKILL.md.
- **SKILL-15 benchmark cases for a convention file** — each case is `task` (terse, deliberately under-specified — „that's when the model's defaults leak"), `antipattern` (the habit the task is designed to elicit), `tell` (the concrete observable the judge looks for); „A fully specified task tests whether the model can follow instructions; an under-specified one tests what it does when the instructions run out, which is what a conventions file is for." Real cases from the chapter: defensive try/except around a happy-path `json.load`; `# type: ignore` instead of fixing the Optional; comments that restate code; a full Args/Returns/Raises docstring on a one-liner; a new `utils.py` when a fitting module exists; class-wrapping one function; a factory for one implementation; a back-compat shim with no external users; reformatting unrelated code in a bugfix; re-implementing an existing helper; re-wrapping an exception and losing the traceback; a test that asserts only on the mock; a TODO while claiming done; a self-congratulatory checkmark summary. `skills/dspy-book-coding-agents/SKILL.md:66-89` [recipe] → here: job 4 cases = „loaded the skill and still did the wrong thing".
- **SKILL-16 the test-agents-md loop** — two LM calls per rollout (the agent writes a plan and code under the candidate as system prompt; the judge returns `VERDICT: <PASS|FAIL>`); deliberately weak seed „You are a careful engineering assistant. Be helpful and concise."; baseline before, all 20 re-scored after, delta and regression list printed; 80 metric calls in the notebook, 200 suggested for real use. `skills/dspy-book-coding-agents/reference.md:66-77` [recipe][number].
- **SKILL-17 Pareto per example** — „Pareto selection runs per example, so a candidate cannot win by collapsing to one register and abandoning the rest" (the persona set's heterogeneity relies on it). In 0.1.4 `optimize_anything` defaults to `frontier_type="hybrid"` (per validation example **and** per objective from `side_info["scores"]`); `gepa.optimize` defaults to `"instance"`; `"cartesian"` is per example × objective. `skills/dspy-book-coding-agents/SKILL.md:58`, `skills/dspy-book-coding-agents/reference.md:82-86` [api] (verified `gepa/core/state.py:22-23`, `gepa/api.py:58`, `gepa/optimize_anything.py:482`).
- **SKILL-18 budgets the chapter reports** — smoke run `max_metric_calls=3` per notebook; persona (clawsona, 10 prompts, 8-rule rubric): 0.962 composite rubric adherence at 50 metric calls, ~19 minutes; landing page: 200 metric calls, ~30 minutes, ~$0.10 per rollout; test-agents-md: 80 (200 suggested); image CLI via `dspy.GEPA`: `max_metric_calls=600`. „These are small budgets producing real but bounded gains, on benchmarks the authors built. Measure your own baseline; do not import theirs." `skills/dspy-book-coding-agents/SKILL.md:141-147`, `skills/dspy-book-coding-agents/reference.md:40-42,62,77,89-90,97` [number][claim].
- **SKILL-19 applying it to a skill pack** — (1) capture real failures where an agent loaded a skill and still did the wrong thing; (2) write each as task/antipattern/tell, under-specified; (3) score binary with a judge that raises on an unparseable verdict; (4) add every deterministic check (frontmatter, length, required sections) and weight them above the judge; (5) optimize, re-score, read the regressions, restore what the benchmark did not cover. „Steps 1, 2 and 4 are worth doing even if you never run the optimizer: a convention file with a benchmark attached is falsifiable, and one without is an opinion." `skills/dspy-book-coding-agents/reference.md:146-158` [recipe] → here: job 4 and `check_skills.py`.
- **SKILL-20 the example's composite** — `composite_score(mechanical_checks, judge_wins) = 0.5 * mean(mechanical) + 0.5 * mean(judge_wins)` (two judges averaged = the chapter's 0.25 + 0.25); `side_info_is_useful` accepts a dict only if some key contains „reason" or „judge" — a naming heuristic, not a content check. `skills/dspy-book-coding-agents/example_artifact_optimizer.py:21-22,90-94,103-106` [recipe].
- **SKILL-21 descriptions drive auto-invocation** — „Agent Skills auto-discovery is description-keyword driven; you don't need exact matches, but the phrasing should overlap with how a user would describe the task." `articles/02-how-to-install-and-use.md:151` [claim] → here: job 4 optimizes exactly the `description` field.
- **SKILL-22 discovering a skill instead of writing one** — see RLM-4; the output is „a candidate for human review before install, which then feeds the optimization loop above". `skills/dspy-book-coding-agents/SKILL.md:132-139` [pattern].
- **SKILL-23 installing and invoking skills (article 02)** — four paths: Claude Code marketplace (`/plugin marketplace add intertwine/dspy-agent-skills`, `/plugin install dspy-agent-skills@dspy-agent-skills`, cached under `~/.claude/plugins/cache/`, manifest `.claude-plugin/plugin.json`); `npx skills add intertwine/dspy-agent-skills --skill '*' -a codex -y` (the bare package-style name does not resolve); `./scripts/install.sh` symlinking `skills/*` into `~/.claude/skills/` and `~/.agents/skills/` (flags `--copy`, `--claude-only`, `--codex-only`, `--uninstall`, `--dry-run`; symlinks make repo edits live); manual `cp -R`. Both harnesses auto-discover any directory containing a `SKILL.md`; explicit invocation is `/name` in Claude Code and `$name` in Codex CLI. The author's bar: install and smoke-test in under five minutes; a full GEPA run on the live examples takes 20–40 minutes. `articles/02-how-to-install-and-use.md:23-71,94-101`, `articles/01-why-dspy-agent-skills.md:65` [recipe][claim] → here: P6's `.claude/skills` → `.agents/skills` symlink is the same layout.

## TRAP

- **TRAP-1 „twelve optimizers" are eleven plus a baseline** — the description says „twelve optimizers on the same detector"; the table has eleven optimizers and the unoptimized row, and the example prints „11 optimizer runs recorded". `skills/dspy-book-optimizers/SKILL.md:5`, `skills/dspy-book-optimizers/example_optimizer_results.py:45-57` [trap] (verified dry run).
- **TRAP-2 `from dspy.datasets import HotPotQA, GSM8K` fails on 3.3.1** — `ImportError: cannot import name 'GSM8K' from 'dspy.datasets'`; use `from dspy.datasets.gsm8k import GSM8K`. `skills/dspy-book-datasets/reference.md:9` [trap] (verified).
- **TRAP-3 `dspy.load("./ai_detector/")` fails on 3.3.1** — `ValueError: Loading with pickle is not allowed. Please set `allow_pickle=True` if you are sure you trust the source of the model.` The chapter's save/reload of the optimized judge needs `dspy.load(path, allow_pickle=True)` (or `save_program=False` plus `Module.load`). `skills/dspy-book-eight-steps/reference.md:56-57` [trap] (verified `dspy/utils/saving.py:27-41`).
- **TRAP-4 `load_state` silently drops endpoint keys** — per-predictor LM state loses `api_base`, `base_url` and `model_list` on load unless `allow_unsafe_lm_state=True`, with the warning „Ignoring unsafe LM config key(s) during state load: … Pass allow_unsafe_lm_state=True to preserve these keys for trusted files." A program optimized against an OpenRouter-style endpoint and reloaded with `load_state(dump_state())` points at the default endpoint. `skills/dspy-book-coding-agents/reference.md:109-111` [trap] (verified `dspy/predict/predict.py:22-40`).
- **TRAP-5 „GEPA was the only optimizer where validation and test agreed exactly" is false by the skill's own table** — BootstrapFinetune is 70.00 / 70.00 too; the example asserts `gepa.overfit_gap == 0.0` with the message „GEPA was the only run where validation matched test" but never checks uniqueness (BootstrapFinetune's gap is also 0.0). `skills/dspy-book-optimizers/SKILL.md:66-67`, `skills/dspy-book-optimizers/reference.md:45`, `skills/dspy-book-optimizers/example_optimizer_results.py:108-109` [trap] (verified table.py).
- **TRAP-6 „beat six paid optimizers" matches nothing** — by test accuracy both free optimizers beat 5 paid runs (BootstrapRS, COPRO, MIPROv2, SIMBA, BetterTogether); KNNFewShot alone beats 8; LabeledFewShot alone beats 5. The example only asserts „≥ 5". `skills/dspy-book-optimizers/SKILL.md:60-62`, `skills/dspy-book-optimizers/example_optimizer_results.py:99-102` [trap] (verified table.py).
- **TRAP-7 `Evaluate` turns a crash into a zero** — a program exception becomes `failure_score` (default 0.0) for that row, logged at ERROR, and the run continues until `max_errors` (settings default 10) is exceeded, then `Exception: Execution cancelled due to errors or interruption.` An unreachable provider and a wrong answer both score 0.0 in `EvaluationResult.score`. Not mentioned anywhere in the slice. [trap] (verified behave4.py T19: 3 failing rows → score 0.0, per-row `[0.0, 0.0, 0.0]`; behave5.py: 12 failing rows → exception) → here: P15; `lmrun.py` records status per call, `Evaluate` must not be the only record.
- **TRAP-8 BestOfN returns None** — every attempt's exception is caught and `print`ed to stdout (not logged); with `N=1` or `N=2` and all attempts failing, BestOfN returns `None`; with `N≥3` it raises the last exception after all calls are spent. Passing the five-argument metric as `reward_fn` produces exactly this (`'dict' object has no attribute 'is_ai'`). The skill says only „the mismatch fails at runtime". `skills/dspy-book-eight-steps/SKILL.md:72-74` [trap] (verified behave1.py T5; `dspy/predict/best_of_n.py:75-83`) → here: P19 — assert non-empty output.
- **TRAP-9 the example's `vote()` differs from `dspy.majority`** — `example_module_choice.py`'s stand-in uses identity normalization by default, so „NYC"/„nyc" split; `dspy.majority`'s default `normalize_text` already folds case and punctuation (it returned „NYC" with three votes on the same list). The lesson (fold aliases like „New York City") holds; the „without normalization" demonstration does not describe DSPy. `skills/dspy-book-modules/example_module_choice.py:71-78,126-132` [trap] (verified behave2.py T7).
- **TRAP-10 `dspy.Parallel`: dicts work, a missing `with_inputs` returns None** — the anti-pattern „`dspy.Parallel` with raw dicts instead of `Example.with_inputs(...)`" is wrong on 3.3.1: `(module, {"question": "q"})` runs (`module(**dict)`). What fails silently is an `Example` without `with_inputs`: the pair's error is logged („Inputs have not been set for this example. Use `example.with_inputs()` to set them.") and its slot in the result list is `None`. `skills/dspy-book-modules/SKILL.md:109-110,132` [trap] (verified behave2.py T6; `dspy/predict/parallel.py:94-100`).
- **TRAP-11 a synchronous ReAct call with async tools does not raise** — `Tool.__call__` raises the ValueError, but ReAct catches every tool exception and stores it as the observation (`Execution error in lookup: Traceback …`), emits `RuntimeWarning: coroutine 'lookup' was never awaited`, and goes on to produce an answer without the tool's data. The book's sentence („raises `ValueError` by default"), the skill's „it raises by design" and the example's `mcp_call_is_legal` all describe a hard failure that does not happen at the agent level; `await agent.acall(...)` returned `sunny in Oslo` in the same probe. `skills/dspy-book-agents/SKILL.md:38-41,122`, `skills/dspy-book-agents/reference.md:26-29`, `skills/dspy-book-agents/example_agent_budget.py:87-92` [trap] (verified behave2.py T9; `dspy/predict/react.py:112-115`) → here: any tool-using script must inspect observations for `Execution error`.
- **TRAP-12 role/content history fabricates answers** — `dspy.History(messages=[{"role": "user", "content": "What is 2+2?"}, {"role": "assistant", "content": "4"}])` raises nothing: both messages render as **assistant** turns reading `[[ ## answer ## ]]\nNone`, and the text „2+2" never reaches the prompt. Field-keyed history renders as user/assistant pairs as intended. `skills/dspy-book-agents/SKILL.md:54-56,124` [trap] (verified behave2.py T8, behave2b.py).
- **TRAP-13 Refine does not fail when the guardrail never passes** — after N below-threshold attempts Refine returns the best of them; wrapping a guardrail in `dspy.Refine(..., reward_fn=passes_guardrail, threshold=1.0)` returns an output that failed the guardrail, with no flag; each miss also costs an `OfferFeedback` call that sends the module and reward-function source code to the LM. Re-check the guard on the returned value. `skills/dspy-book-production/SKILL.md:61-64`, `skills/dspy-book-production/reference.md:41-42` [trap] (verified `dspy/predict/refine.py:139-177`).
- **TRAP-14 gepa's default reflection model is a live paid call** — see SKILL-12: `ReflectionConfig.reflection_lm = "openai/gpt-5.1"`; the book's snippet always passes one, but a config built without it calls OpenAI through litellm. [trap] (verified `gepa/optimize_anything.py:742`).
- **TRAP-15 a misspelt `with_inputs` name is accepted silently** — `dspy.Example(text="x", label="y").with_inputs("question")` raises nothing; `.inputs()` is empty and the program gets no input. The datasets example's stand-in raises `KeyError` for the same call and prints „with_inputs rejects a field that does not exist" — a guarantee DSPy does not give. `skills/dspy-book-datasets/example_dataset_builder.py:33-38,112-117`, `skills/dspy-book-datasets/SKILL.md:44-45` [trap] (verified behave1.py T2) → here: assert `set(ex.inputs().keys()) == expected` after conversion.
- **TRAP-16 the pipeline metric's dual-mode test breaks BootstrapFewShot** — `if trace is None and pred_name is None: return is_correct` routes BootstrapFewShot's call (trace present, `pred_name=None`) into the optimizer branch, where `pred_trace[-1]` raises `TypeError: 'NoneType' object is not subscriptable`; BootstrapFewShot aborts after 10 metric errors. The eight-steps shape (`if pred_name: … return score`) compiles fine; under `dspy.GEPA` both shapes work because GEPA's module-level call passes `trace=None`. Branch on `pred_name`, never on `trace`. `skills/dspy-book-metrics/SKILL.md:108-115`, `skills/dspy-book-metrics/reference.md:145-155` [trap] (verified boot1.py, gepa1.py) → here: `pairs.py` metric shape.
- **TRAP-17 book defects the skills record (unverifiable here)** — the chapter-6 README claims `gepa.ipynb` has a custom `WordLimitProposer`: „No such symbol exists anywhere in chapter 6"; `bootstrap-finetune.ipynb` says „DSPy 3.2.1's `LocalProvider`" while the repo pins 3.3.0 („the TRL keyword shim is version-sensitive"); chapter 3 evaluates on the full dataset including training rows; the quickstart optimizes against a slice of its own test set; field keyword usage mixes `desc=`/`description=`; one chapter-4 notebook uses `dspy.settings.configure`. `skills/dspy-book-optimizers/reference.md:137-142`, `skills/dspy-book-eight-steps/reference.md:92-98`, `skills/dspy-book-datasets/reference.md:132-138` [claim].
- **TRAP-18 model identifiers** — „Model identifiers throughout these notebooks are forward-dated placeholders that do not resolve against any provider" (eight-steps, optimizers, modules, coding-agents), while the metrics reference calls `openai/gpt-5-mini` and `openai/gpt-5.5` „the book's choices; verify against what your account actually serves", and the modules reference says they „contradict the notebooks' own setup cells". `skills/dspy-book-eight-steps/SKILL.md:93-94`, `skills/dspy-book-metrics/reference.md:182-183`, `skills/dspy-book-modules/reference.md:119-120`, `skills/dspy-book-optimizers/reference.md:141` [claim].
- **TRAP-19 the percentage parser keeps the percent scale** — `parse_number("-2.8%")` returns `-2.8`, not `-0.028`; a gold value stored as a fraction never matches under `rel_tol=0.05`, and the metric returns 0 without calling the judge. Suffix detection is also case-folded, so a trailing `m`/`M` means million. `skills/dspy-book-use-cases/example_use_case_router.py:61-71,116` [trap] (verified dry run assertion `parse_number("-2.8%") == -2.8`).
- **TRAP-20 the article reports the selection-set score as final** — „The committed scores are GEPA's own cached full-valset evaluations (used for candidate selection), which is what you'd want: the same number GEPA optimized against." That is the number the optimizers chapter says not to report („Report the held-out number"; MIPROv2's 10.42-point gap); the committed invoice `results.json` says the final fresh re-evaluation „could not complete". `articles/03-inside-the-examples.md:112`, `skills/dspy-book-optimizers/SKILL.md:65-67`, `examples/03-invoice-extraction/results.json` [trap] (verified the notes field).
- **TRAP-21 article 03's table is not what the repo records** — article: RAG QA GLM 4.5 Air 81.15 → 100.00; math LFM 1.2B 45.00 → 70.00 (+25, the title's claim); invoice 0.833 → 0.931; „free OpenRouter models only, a single reflection LM (Nemotron-3-super-120B-a12b). Total spend … zero dollars". Committed at 9d13f98: RAG `ministral-3b-2512` (paid) + reflection `qwen3-30b-a3b-instruct-2507`, 80.47 → 100.0 (DSPy 3.2.0, 829.4 s); math same pair, 85.0 → 93.33 (210.7 s); only invoice matches (LFM 1.2B + Nemotron, 0.8333 → 0.9315, 20 train / 9 val, 5 accepted). The 45 → 70 run survives as the „Historical, 3.1.3" row of `examples/02-math-reasoning/version_comparison.md`. `articles/03-inside-the-examples.md:2,14-20` [trap] (verified `examples/*/results.json`, `version_comparison.md`).
- **TRAP-22 single-seed gains sit inside run-to-run spread** — invoice extraction with the same model pair and data: committed baseline 0.833, a clean 3.1.3 probe 0.739 (interrupted at 162/416 rollouts after 28 min 29 s, best observed 0.944), a clean 3.2.0 baseline 0.944 — a baseline spread of 0.205 against the reported GEPA gain of 0.098; RAG QA with one model pair: 86.10 → 91.75 on DSPy 3.1.3 vs 80.47 → 100.00 on 3.2.0. The articles concede „a few points of drift". `articles/03-inside-the-examples.md:22,126` [number] (verified `examples/01-rag-qa/version_comparison.md`, `examples/03-invoice-extraction/version_comparison.md`) → here: P18 — repeats before any claim.
- **TRAP-23 article 04 calls Ensemble weight-level optimization** — „DSPy 3.x supports weight-level optimization (`BetterTogether`, `dspy.Ensemble`)"; Ensemble combines already-built programs at inference time (OPT-23); the weight optimizer is BootstrapFinetune. `articles/04-future-use-cases.md:83` [trap].
- **TRAP-24 the articles describe another pack** — five skills (`dspy-fundamentals`, `dspy-evaluation-harness`, `dspy-gepa-optimizer`, `dspy-rlm-module`, `dspy-advanced-workflow`), „You should see five directories", „60 assertions", v0.1.0, `intertwine/...` install commands; this repository has 32 skill directories and plugin version 0.11.0. `articles/01-why-dspy-agent-skills.md:53-59,67`, `articles/02-how-to-install-and-use.md:86`, `articles/04-future-use-cases.md:12-18` [trap] (verified `ls skills/`, `.claude-plugin/plugin.json`).
- **TRAP-25 article 02's error text** — „*"reflection_lm cannot be None"*" is not the 3.3.1 message; the assertion reads „GEPA requires a reflection language model, or custom instruction proposer to be provided. …" (the timing claim, at construction, is right). `articles/02-how-to-install-and-use.md:147` [trap] (verified behave4.py T16).
- **TRAP-26 other trap-tagged items in this file** — API-11 (silent JSON fallback call), API-12 (TwoStep extraction not optimizable), API-13 (`dspy.BAMLAdapter`), API-16 (`dspy.Image` path, `from_url`), API-17 (`soundfile`), API-22 (bound methods rejected by CodeAct), API-35 (ReAct docstring default), API-44 (English-only normalization), OPT-7 (COPRO's validation column is its search set), OPT-15 (KNNFewShot deferral and lost retrieval), OPT-21, OPT-22, OPT-24 (no demo stacking), DATA-15 (DummyVectorizer reseed), PROD-3, PROD-6, RAG-2, RAG-3, TEST-2 (dry-runs cannot fail on DSPy drift), SKILL-5, SKILL-8, SKILL-9, SKILL-11. [trap]

## Code worth keeping

**1. The dual-mode metric — the safe shape.** `skills/dspy-book-eight-steps/reference.md:35-41`. Runs on 3.3.1: this predicate (`if pred_name:`) compiled under BootstrapFewShot and ran under `dspy.GEPA` in my offline probes (boot1.py, gepa1.py); `Evaluate` gets the plain score.
```python
def exact_match(example, response, trace=None, pred_name=None, pred_trace=None):
    score = 1 if example.is_ai == response.is_ai else 0
    if pred_name:
        return dspy.Prediction(score=score, feedback=example.notes)
    return score
```

**2. The pipeline metric — keep the body, change the first test.** `skills/dspy-book-metrics/reference.md:145-155`. As written it **breaks BootstrapFewShot** on 3.3.1 (TRAP-16, verified); replace the first condition with `if pred_name is None:`. The trace element shape `(predictor, inputs, outputs)` is verified.
```python
def pipeline_metric_with_feedback(example, pred, trace=None, pred_name=None, pred_trace=None):
    if trace is None and pred_name is None:
        return is_correct                        # plain evaluation
    _, inputs, outputs = pred_trace[-1]
    if pred_name == "generate_query":
        ...
    elif pred_name == "synthesize":
        ...
    return dspy.Prediction(score=score, feedback=feedback)
```

**3. Judge calibration.** `skills/dspy-book-metrics/reference.md:96-113` and the promotion `skills/dspy-book-metrics/SKILL.md:75-81`. Parameters verified against 3.3.1 signatures; not executed against a model.
```python
class JudgeQuality(dspy.Signature):
    """Evaluate whether a model's answer is accurate and helpful."""
    question: str = dspy.InputField()
    answer: str = dspy.InputField()
    gold_answer: str = dspy.InputField(desc="The correct answer for reference")
    is_good: bool = dspy.OutputField(desc="True if answer is accurate and helpful")
    reasoning: str = dspy.OutputField(desc="Explain your judgment")

def judge_metric(example, pred, trace=None):
    return pred.is_good == example.is_good          # plain agreement, no kappa

optimizer = dspy.BootstrapFewShot(metric=judge_metric,
                                  max_bootstrapped_demos=8, max_labeled_demos=8)
split = max(1, int(len(judge_trainset) * 0.8))
optimized_judge = optimizer.compile(student=dspy.ChainOfThought(JudgeQuality),
                                    trainset=judge_trainset[:split])

def llm_judge_metric(example, pred, trace=None, pred_name=None, pred_trace=None):
    result = optimized_judge(question=example.question, answer=pred.answer,
                             gold_answer=example.answer)
    return dspy.Prediction(score=1.0 if result.is_good else 0.0,
                           feedback=result.reasoning)
```

**4. Guards that inspect a callable before it is used.** `skills/dspy-book-eight-steps/example_eight_steps.py:54-60` and `87-93`; pure Python, pass their dry run; the 2-parameter reward shape matches `dspy/predict/best_of_n.py:67` (verified).
```python
def metric_is_optimizer_ready(fn) -> tuple[bool, str]:
    """A three-argument metric must be rewritten before reflective optimization."""
    params = list(inspect.signature(fn).parameters)
    if len(params) >= METRIC_ARITY_FOR_OPTIMIZER:
        return True, f"{len(params)} parameters; usable by Evaluate and an optimizer"
    return False, (f"{len(params)} parameters; widen to "
                   f"(example, pred, trace=None, pred_name=None, pred_trace=None) now, it costs nothing")

def best_of_n_reward_shape_ok(fn) -> tuple[bool, str]:
    """BestOfN.reward_fn takes (kwargs_dict, pred) — NOT the metric signature."""
    params = list(inspect.signature(fn).parameters)
    if len(params) == 2:
        return True, "two positional parameters: (kwargs, pred)"
    return False, (f"{len(params)} parameters; BestOfN passes a kwargs dict and a prediction, "
                   f"not the metric's argument list")
```

**5. Metric-to-reward bridge.** `skills/dspy-book-eight-steps/reference.md:65-72`. Shape verified (reward_fn receives the input kwargs); works only for metrics that need no gold field (PAT-4).
```python
def _best_of_n_reward(kwargs, pred):            # NOT the metric signature
    dummy = dspy.Example(**kwargs).with_inputs(*kwargs.keys())
    return float(optimized_llm_judge(dummy, pred))

selector = dspy.BestOfN(module=optimized_transformer, N=5,
                        reward_fn=_best_of_n_reward, threshold=1.0)
```

**6. Seeded split and the leakage check.** `skills/dspy-book-datasets/example_dataset_builder.py:53-70`; pure Python, dry run verified (a 2-row overlap is caught). `Example.uid()` is content-based (DATA-14).
```python
def split(examples: list[Example], seed: int = SEED) -> tuple[list, list, list]:
    """Seeded and deterministic, or no two runs are comparable."""
    shuffled = list(examples)
    random.Random(seed).shuffle(shuffled)
    n = len(shuffled)
    train_end = int(TRAIN_FRACTION * n)
    val_end = train_end + int(VAL_FRACTION * n)
    return shuffled[:train_end], shuffled[train_end:val_end], shuffled[val_end:]


def splits_are_disjoint(train: list[Example], val: list[Example],
                        test: list[Example]) -> tuple[bool, str]:
    """The check chapter 4 never makes and chapter 3 actually violates."""
    t, v, s = {e.uid() for e in train}, {e.uid() for e in val}, {e.uid() for e in test}
    for a, b, names in ((t, v, "train/val"), (t, s, "train/test"), (v, s, "val/test")):
        if a & b:
            return False, f"{names} overlap on {len(a & b)} example(s): leakage"
    return True, "train, validation and test are disjoint"
```

**7. Partial credit, gradient test, rubric normalization.** `skills/dspy-book-metrics/example_metric_recipes.py:45-52`, `66-71`, `93-95`; pure Python, dry run verified (best 1.00, worst 0.00, bad weights raise).
```python
def keyword_partial(answer: str, keywords: list[str]) -> float:
    """Partial credit: an optimizer can see progress between 0 and 1."""
    if not keywords:
        return 1.0
    lowered = answer.lower()
    hits = sum(bool(re.search(KEYWORD_BOUNDARY.format(re.escape(k.lower())), lowered))
               for k in keywords)
    return hits / len(keywords)

def normalize_rubric(scores: dict[str, int], weights: dict[str, float]) -> float:
    """Weighted 1-5 rubric mapped onto 0-1, as the chapter does it."""
    if not math.isclose(sum(weights.values()), 1.0, abs_tol=1e-9):
        raise ValueError(f"weights must sum to 1.0, got {sum(weights.values())}")
    weighted = sum(scores[k] * w for k, w in weights.items())
    return (weighted - RUBRIC_MIN) / (RUBRIC_MAX - RUBRIC_MIN)

def gives_gradient(scores: list[float]) -> bool:
    """A metric an optimizer can climb takes more than two distinct values."""
    return len(set(scores)) > 2
```

**8. Deterministic gate before the judge.** `skills/dspy-book-use-cases/example_use_case_router.py:74-90` (the parser at `61-71` keeps percent scale, TRAP-19); pure Python, dry run verified (no judge call on wrong or unparseable answers).
```python
def graded_metric(gold_value: float, predicted_text: str, *,
                  poisoning_detected: bool = False, drift_detected: bool = False,
                  judge_calls: list | None = None) -> float:
    """Deterministic gate first; the judge is only called on a correct answer."""
    judge_calls = judge_calls if judge_calls is not None else []
    try:
        value = parse_number(predicted_text)
    except ValueError:
        return 0.0                                  # unparseable: no judge call
    if not math.isclose(value, gold_value, rel_tol=NUMERIC_TOLERANCE):
        return 0.0                                  # wrong: no judge call
    judge_calls.append(predicted_text)              # only correct answers reach the judge
    if poisoning_detected:
        return POISONED_SCORE
    if drift_detected:
        return DRIFTED_SCORE
    return CLEAN_SCORE
```

**9. The optimizer invocations.** `skills/dspy-book-optimizers/SKILL.md:76-91` and `skills/dspy-book-optimizers/reference.md:85-88`; every keyword verified against 3.3.1 signatures; not executed with a model (KNNFewShot's behaviour at inference: OPT-15).
```python
dspy.LabeledFewShot(k=4).compile(detector, trainset=trainset, sample=True)

dspy.BootstrapFewShot(metric=exact_match, max_bootstrapped_demos=2,
                      max_labeled_demos=2, max_rounds=1, max_errors=1
                      ).compile(detector, trainset=trainset)

dspy.KNNFewShot(k=4, trainset=trainset, vectorizer=vectorizer, metric=exact_match,
                max_bootstrapped_demos=0, max_labeled_demos=4).compile(detector)

dspy.GEPA(metric=feedback_metric, auto='light', reflection_minibatch_size=3,
          reflection_lm=reflection_lm, num_threads=4,
          candidate_selection_strategy='pareto', use_merge=False,
          track_best_outputs=True, track_stats=True, seed=42
          ).compile(detector, trainset=trainset, valset=valset)

def boolean_majority(predictions):
    return dspy.majority(predictions, field="is_ai", normalize=lambda v: str(v).lower())

dspy.Ensemble(reduce_fn=boolean_majority).compile([labeled, bootstrapped, searched])
```

**10. The optimize_anything call.** `skills/dspy-book-coding-agents/SKILL.md:32-51`; names verified in gepa 0.1.4; runs offline when `REFLECTION_MODEL` is a callable (TEST-9). Two changes for 0.1.4: drop „no markdown fences" (SKILL-8) and pass a `valset` (SKILL-9).
```python
from gepa.optimize_anything import optimize_anything, GEPAConfig, EngineConfig, ReflectionConfig

def evaluator(candidate: str, example: dict) -> tuple[float, dict]:
    response = TASK_LM(messages=[{"role": "system", "content": candidate},
                                 {"role": "user", "content": example["prompt"]}])[0]
    score, verdict = judge(response, example)
    return score, {"Prompt": example["prompt"], "Response": response,
                   "JudgeReasoning": verdict}          # the reflector's only window into why

result = optimize_anything(
    seed_candidate=open("SKILL.md").read(),
    evaluator=evaluator,
    dataset=CASES,
    objective="Rewrite the skill so ... Output ONLY the SKILL.md text. No preamble, no markdown fences.",
    config=GEPAConfig(engine=EngineConfig(max_metric_calls=80),
                      reflection=ReflectionConfig(reflection_lm=REFLECTION_MODEL)),
)
print(result.best_candidate)      # the new file text
```

**11. Guards for the artifact loop.** `skills/dspy-book-coding-agents/example_artifact_optimizer.py:61-69`, `82-87`, `97-100`; pure Python, dry run verified; `evaluator_signature_ok` matches gepa 0.1.4's name-based injection except for `**kwargs` evaluators (SKILL-4).
```python
def evaluator_signature_ok(fn) -> tuple[bool, str]:
    """optimize_anything reads the evaluator's parameter NAMES, not positions."""
    params = list(inspect.signature(fn).parameters)
    if len(params) < 2:
        return False, "evaluator needs (candidate, example)"
    if params[1] != REQUIRED_EVALUATOR_PARAM:
        return False, (f"second parameter is {params[1]!r}, must be "
                       f"{REQUIRED_EVALUATOR_PARAM!r}; GEPA silently drops the dataset otherwise")
    return True, "signature accepted"

def parse_verdict(text: str) -> bool:
    """Binary verdict; an unparseable judge raises rather than scoring zero."""
    match = VERDICT_RE.search(text)
    if not match:
        raise ValueError(f"judge returned no parseable verdict: {text[:60]!r}")
    return match.group(1).upper() == "PASS"

def regressions(cases: list[BenchmarkCase], baseline: list[float],
                optimized: list[float]) -> list[str]:
    """GEPA drops rules that never fired. This is how you find them."""
    return [c.antipattern for c, b, o in zip(cases, baseline, optimized) if b > o]
```

**12. Randomized-position pairwise judge.** `skills/dspy-book-coding-agents/reference.md:57-60`; pure Python fragment.
```python
swap = random.random() < 0.5
candidate_won = (verdict == ("A" if not swap else "B"))
```

**13. Trajectory reading and history hygiene.** `skills/dspy-book-agents/example_agent_budget.py:59-84`; pure Python, dry run verified; the `tool_name_{i}` key format matches ReAct 3.3.1 (API-35).
```python
def read_trajectory(trajectory: dict, limit: int = 20) -> list[str]:
    """Iterate an index range and break on the first missing key."""
    names = []
    for i in range(limit):
        key = f"tool_name_{i}"
        if key not in trajectory:
            break
        names.append(trajectory[key])
    return names


def history_entry(result: dict) -> dict:
    """Store the answer, never the trajectory: otherwise every tool payload replays."""
    if "trajectory" in result and "answer" not in result:
        raise ValueError("refusing to store a trajectory in history; store the answer")
    return {"question": result["question"], "answer": result["answer"]}


def history_message_keys_ok(message: dict, signature_fields: set[str]) -> tuple[bool, str]:
    """dspy.History keys mirror the signature's fields, not role/content."""
    if {"role", "content"} & set(message):
        return False, "role/content is the OpenAI shape; dspy.History keys are signature field names"
    unknown = set(message) - signature_fields
    if unknown:
        return False, f"keys {sorted(unknown)} are not fields of the signature"
    return True, "keys match the signature fields"
```

**14. Typed-only fallback.** `skills/dspy-book-production/example_serving_checks.py:20-21,42-48`; pure Python, dry run verified.
```python
# Exceptions a fallback may catch. Anything else is your bug, not the provider's.
FALLBACK_SAFE = {"APIError", "RateLimitError", "ServiceUnavailableError", "Timeout"}

def fallback_is_safe(caught: set[str]) -> tuple[bool, str]:
    if "Exception" in caught or "BaseException" in caught:
        return False, "bare Exception turns your own bugs into a silent downgrade to a weaker model"
    unknown = caught - FALLBACK_SAFE
    if unknown:
        return False, f"{sorted(unknown)} are not provider failures; catch typed errors only"
    return True, "typed provider failures only"
```

**15. Offline `optimize_anything` fixture — not from the repo**, written for this extraction and run as shown on gepa 0.1.4 (`verify/fixture_oa.py`: printed `[0.5, 1.0] 10`). The callable reflection LM keeps litellm out; `valset` gives a held-out set; the objective asks for one fenced block.
```python
from gepa.optimize_anything import optimize_anything, GEPAConfig, EngineConfig, ReflectionConfig

CASES = [{"prompt": "p-a", "needs": "Rule A"}, {"prompt": "p-b", "needs": "Rule B"}]

def evaluator(candidate: str, example: dict):          # 2nd parameter MUST be named `example`
    ok = example["needs"] in candidate
    return (1.0 if ok else 0.0), {"Prompt": example["prompt"],
                                  "JudgeReasoning": "ok" if ok else f"missing {example['needs']}"}

def offline_reflection_lm(prompt) -> str:              # any callable prompt -> str; never litellm
    return "```\nRule A. Rule B.\n```"                 # gepa extracts between first and last ```

result = optimize_anything(
    seed_candidate="Rule A.", evaluator=evaluator, dataset=CASES, valset=CASES,
    objective="Rewrite the rules file. Output ONLY the file text, inside one ``` block.",
    config=GEPAConfig(engine=EngineConfig(max_metric_calls=8, parallel=False),
                      reflection=ReflectionConfig(reflection_lm=offline_reflection_lm)))
assert result.best_candidate == "Rule A. Rule B.", result.best_candidate
```

**16. Offline `dspy.GEPA` fixture — not from the repo**, run as shown on DSPy 3.3.1 (`verify/fixture_gepa.py`: printed `[0.5]`). DummyLM serves both the task and the reflection LM; the metric branches on `pred_name`.
```python
import dspy
from dspy.utils.dummies import DummyLM

class Sig(dspy.Signature):
    """Answer yes or no."""
    question: str = dspy.InputField()
    answer: str = dspy.OutputField()

def metric(example, pred, trace=None, pred_name=None, pred_trace=None):
    score = float(example.answer == pred.answer)
    if pred_name is None:                       # Evaluate, BootstrapFewShot, GEPA module-level
        return score
    return dspy.Prediction(score=score, feedback=f"expected {example.answer!r}, got {pred.answer!r}")

data = [dspy.Example(question=f"q{i}", answer="yes" if i % 2 else "no").with_inputs("question") for i in range(6)]
dspy.configure(lm=DummyLM([{"answer": "yes"}] * 400))
gepa = dspy.GEPA(metric=metric, max_metric_calls=30, num_threads=1, track_stats=True, seed=0,
                 reflection_lm=DummyLM([{"new_instruction": "Answer yes or no."}] * 50))
optimized = gepa.compile(dspy.Predict(Sig), trainset=data[:4], valset=data[4:])
```

## The old report, corrected

`Plan/concept/dspy-repos_2026-09-23/dspy-agent-skills-B.md` (109 lines) — the map was mostly right about *what* the book skills say; it ran nothing, so it repeated the skills' errors and missed every behaviour that only shows up when DSPy runs.

1. **It ran nothing and trusted the dry-runs.** „I did not run any live `uv run` command" (§5); „every book example's dry-run passes on 3.3.1" (§1). The dry-runs do pass — they never import `dspy` (TEST-2), so they cannot say anything about 3.3.1. Running DSPy found four outright API breaks in the slice (TRAP-2, TRAP-3, API-13, API-16).
2. **Item 17 repeats two false statements** — „GEPA is the only run where val==test exactly" (BootstrapFinetune too, TRAP-5) and „the two free optimizers … beat six paid ones" (5 by both, 8 by KNNFewShot, TRAP-6). Also „twelve optimizers" is eleven plus a baseline (TRAP-1).
3. **Item 20 (KNNFewShot) is incomplete** — the `num_threads` error fires at the first *inference*, not at construction or compile; KNNFewShot runs BootstrapFewShot on every call; `save()` loses the retrieval (OPT-15).
4. **Item 34 (BestOfN)** — besides the calling convention, a wrong reward function makes BestOfN return `None` for N ≤ 2 and print (not log) each failure (TRAP-8); BestOfN/Refine force `temperature=1.0` with rollout ids (API-7).
5. **Item 37 (JSONAdapter)** — misleading: ChatAdapter falls back to JSONAdapter automatically on a parse failure, spending a second call (API-11).
6. **Items 54 and 55 (MCP async, History keys)** are wrong in consequence: a sync ReAct call with async tools does not raise, it answers without the tool (TRAP-11); role/content history does not merely fail to help, it injects fabricated „None" answers (TRAP-12). The report tagged them `[skip]` and `[skip/catalogue]`; the first is a silent-failure pattern the target's P15/P19 care about.
7. **Item 26 („No markdown fences")** was tagged `[adopt]`; with gepa 0.1.4 that sentence can truncate any artifact containing a code block (SKILL-8).
8. **§3 „the recipe transfers as-is"** — missing: the default reflection model is a live `openai/gpt-5.1` call (SKILL-12), the evaluator runs in parallel threads (SKILL-11), `dataset` without `valset` has no held-out set (SKILL-9), `raise_on_exception=False` or a defaulted parameter make the name trap fully silent (SKILL-5), the budget can be exceeded (SKILL-13).
9. **§5 dependencies is wrong** — „`gepa` … is a new dependency the target does not yet have … installing it … is the only footprint addition". gepa 0.1.4 is already installed as a DSPy 3.3.1 dependency in `.venv-dspy` (SKILL-3); the version differs from the book's 0.1.1.
10. **§4 on P18 (cache off)** — right that the book skills give no cache-off recipe; the 3.3.1 facts are `dspy.LM(cache=False)`, `DSPY_CACHEDIR`/`DSPY_CACHE_LIMIT`, and `rollout_id` only working with temperature > 0 (API-43).
11. **Item 6 (trace-aware dual-mode metric)** — adopted without noticing that the metrics chapter's `trace is None and pred_name is None` test crashes BootstrapFewShot (TRAP-16); the safe predicate is `pred_name`.
12. **Item 9 (`with_inputs`)** — missed that DSPy accepts a misspelt input name silently, and that the skill's stand-in claims the opposite (TRAP-15).
13. **Articles were in scope and got no item.** Article 03's headline numbers are not the committed results (TRAP-21), single-seed gains sit inside observed baseline spread (TRAP-22), the article recommends reporting GEPA's selection-set score (TRAP-20), article 04 mislabels Ensemble (TRAP-23) and miscounts RepoAuditor's instruction slots (RLM-5), and all four describe the five-skill v0.1.0 pack (TRAP-24).
14. **Missing entirely:** `Evaluate` folding exceptions into 0.0 (TRAP-7), `Parallel` returning `None` and accepting dicts (TRAP-10), Refine returning a failing output under a guardrail (TRAP-13), `load_state` dropping endpoint keys (TRAP-4), `dspy.load` needing `allow_pickle` (TRAP-3), `from dspy.datasets import GSM8K` failing (TRAP-2), „recompiling stacks demos" being false on 3.3.1 (OPT-24), GEPA's auto-budget numbers (OPT-17), `skip_perfect_score` as the mechanism of the null results (OPT-19), COPRO's validation column being its search set (OPT-7), the McNemar p being reproducible from the table (OPT-9), and that offline `dspy.GEPA` and `optimize_anything` runs are possible with DummyLM or a callable reflection LM (TEST-8, TEST-9).
15. **Item 38 (RLM ≈ 50k tokens)** is fine as a claim; add that `max_llm_calls` bounds sub-LLM calls, not iterations, and that the sandbox's read/write/env/network can be opened per path via `PythonInterpreter` (RLM-3, API-47); Deno also installs from pip (API-48).
16. **Item 13 (split sizes)** — add that chapter 3 itself uses a global `random.seed(42)` and a 50/50 train/val split with no test set (DATA-3).

## Ten things the skill must say

1. Branch a dual-mode metric on `pred_name`, never on `trace`: BootstrapFewShot passes a trace with `pred_name=None` and the metrics chapter's pipeline shape crashes it (TRAP-16, API-2).
2. A metric returns a number or `dspy.Prediction(score, feedback)`; a dict crashes `Evaluate` after the run is paid for, and a float gives GEPA only „This trajectory got a score of X" (API-3, OPT-18).
3. Measure a baseline and report a held-out number: COPRO (−3.75) and SIMBA (−6.25, the costliest run) lost to the baseline, MIPROv2 lost 10.42 points from validation to test, and the articles report a selection-set score (OPT-14, TRAP-20).
4. Run the free optimizers first — LabeledFewShot and KNNFewShot (+13.75 and +18.75 for $0) and BootstrapFewShot (+13.75 for $0.003) — but KNNFewShot does its work at inference and its retrieval is not saved (OPT-3, OPT-4, OPT-6, OPT-15).
5. DSPy 3.3.1 fails softly in six places — `Evaluate` (0.0), `Parallel` (`None`), `BestOfN` (`None`), `Refine` (a failing output), `ReAct` (error text as observation), ChatAdapter (a silent second call) — so record a status per call instead of trusting the aggregate (TRAP-7, TRAP-10, TRAP-8, TRAP-13, TRAP-11, API-11).
6. `BestOfN`/`Refine` reward functions take `(kwargs, pred)`, not the metric's arguments, and both force `temperature=1.0` with distinct rollout ids (API-6, API-7).
7. `optimize_anything` injects the dataset item by the parameter name `example`; assert the name before calling, and never set `raise_on_exception=False` (SKILL-4, SKILL-5).
8. Configure `optimize_anything` explicitly: a reflection LM (the default is a live `openai/gpt-5.1` call; a Python callable works offline), `max_metric_calls` (it can be exceeded), a `valset` (the book has none), and `parallel=False` for rate-limited evaluators (SKILL-12, SKILL-13, SKILL-9, SKILL-11).
9. Put the side info the reflector needs — the judge's reasoning — in the evaluator's dict, ask for the artifact inside one ``` block instead of „no markdown fences", then read the regression list and restore the rules no case exercised (SKILL-6, SKILL-8, SKILL-14).
10. Trust an LLM judge only after 20–50 human labels with reasons and ≥ 80–90 % held-out agreement (80 % on a balanced binary task is κ ≈ 0.6), and re-measure agreement after optimizing against it (MET-11, MET-13, MET-17).
