# Probe run — four audit documents, cheap merge model, 2026-09-16

The second live `/research-ingest`, run to answer one question: what needs
improving before the remaining documents are ingested. Four documents chosen as
the smallest audit slice, with `konsolidierung-des-hard-canon-protokolls` kept
in deliberately — it is the document that claims authority over other sources,
and audit finding #1 was about the ingest honouring that claim.

Run artifact: `tools/kpwiki/eval_runs/probe-2026-09-16.json` (git-ignored).
Baseline: `pilot-run_2026-09-16.md`.

## What it cost, against the pilot

| | pilot | probe |
|---|---|---|
| sources | 3 (245 lines, 26,760 chars) | 4 (209 lines, 39,408 chars) |
| merge model | `claude/sonnet` | **`claude/haiku`** (`--merge-role worker`) |
| task model | `claude/sonnet` | `claude/sonnet` |
| claims | 141 | **150** |
| concepts | 46 | **41** |
| unassigned claims | 0 | **0** |
| `compile_metric` | 0.986 | **0.967** |

## What it got right

**Contradiction detection worked, and audit finding #1 holds.** Eight
disagreements across seven concepts, **every one of them `pending`**:

| concept | topic |
|---|---|
| `die-13-alter` | total number and canonical roster of System Kael's alters |
| `tsdp` | number of System Kael's alters |
| `aegis` | external antagonist opposed to Kael, or identical with Kael |
| `juna` | internal exiled part of Kael's psyche, or an external force |
| `juna` | when the ontological bond between Juna/V and Kael originated |
| `moonshine-link` | when the Kael–Juna/V bond originated |
| `wahrheitstheorien-achse` | whether the Coherence/Correspondence conflict resolves or stays open |
| `the-foundation` | what the Foundation fundamentally is |

The Hard-Canon document asserts supersession over other sources and the ingest
did not grant it: nothing came back `supersedes`. That is the audit fix
confirmed against live output rather than a fixture.

Two pairs are the same argument reached from different concepts —
`die-13-alter`/`tsdp` on the alter count, `juna`/`moonshine-link` on the bond's
origin. That duplication is why the ledger is keyed by entity as well as by
concept.

## What needs improving, in order

**1. Citations did not resolve — 46 errors, and 40 of them were the lint's
fault.** This looked like the dominant model defect and was mostly a defect in
`rule_citation_resolves`. Diagnosing one case:

    source : … (dem „Nichts-Rauschen“) stammt.      U+201C
    page   : … (dem „Nichts-Rauschen") stammt.      U+0022

The quote is verbatim. The exporter wrote a typographic closing quote and the
model wrote an ASCII one, and the rule compared the glyphs. A second class:
a page writing `the "Moonshine-Link," positioned as…` quotes a source reading
`the "Moonshine-Link" is positioned as…` — same words, and the comma belongs to
the sentence around the quotation, not to the source.

Measured across all 46:

| | |
|---|---|
| matched once quote glyphs were unified | 19 |
| needed glyphs **and** enclosing punctuation stripped | 21 |
| **genuinely wrong** | **6** |

The rule now normalises typographic quotes, apostrophes and dashes to ASCII and
strips punctuation enclosing the fragment before comparing. It is not weaker:
the wording itself must still appear in the cited lines character for character,
an empty or punctuation-only fragment is rejected outright (it would otherwise
be a substring of everything), and `Nichts-Rauschens` still fails against
`Nichts-Rauschen`.

The 6 survivors are real and are the model's: `„living Gödel-Satz“` is in the
source but on line 54 while line 46 was cited, and `„the Foundation“` appears
in no cited file at all. Six fabricated-or-misplaced quotes out of 150 claims
is a different and much smaller problem than 46.

**2. Every source page busts its budget — 4 of 4.** 1,530 · 1,504 · 2,918 ·
2,695 words against a 1,000-word maximum. The ingest writes one source page per
document with every key claim on it, and an audit document yields more claims
than the budget allows. Either the budget is wrong for source pages or the page
must split; both are contract decisions.

**3. `codex_ref` was set on 19 of 22 pages that need it.** `kiko`, `lex` and
`nyx` shared a slug with a codex entry and did not link to it, because the link
was only ever written when the *model* supplied it. A concept page whose slug
is a codex entry is the same subject by identity, so the link is now filled in
as a lookup rather than a judgement. Fixed.

**4. Language discipline slips.** `report-reorienting…: claims left the source
language (en)`, and two definition sentences are not English. Canon prose is
German and engineering English; a claim must stay in its source's language.

## What the probe changed about the repo

The ingest's own lint reported 48 findings; the full `scripts/wiki_lint.py`
finds more, because the runner lints only the pages it just wrote and with a
narrower rule set. The runner's report is a first pass, not the gate.

It also exposed a regression from the Codex restructure. Rule
`no-auto-canon-page` forbids a wiki page that mirrors a file in a terminal
directory, and `Codex/` is terminal. While the codex rendered as three files
named `GLOSSARY`, `MASTER-TIMELINE` and `WORLD-AXIOMS`, the rule could never
fire. Split into one file per entry, every entry slug became a name a concept
page legitimately takes, and the rule raised 26 errors against pages that were
correct. The wiki's research understanding of AEGIS and the codex entry for
AEGIS are different layers meant to coexist, joined by `codex_ref`. The rule
now errors only on `Canon/` and, for a codex slug, warns when `codex_ref` is
missing — which is how the three real gaps above were found.

## What is left, and why it is not fixed here

Twelve errors remain, all in this run's generated drafts rather than in code:
six fabricated or misplaced quotes, two `context_summary` fields over the
40-word limit, and four source pages over the 1,000-word budget. None can be
repaired by editing the files — they are generated, and hand-editing a draft
would only hide what the next run will do again. They need either a re-run with
better guidance, which costs money and is the author's call, or a contract
change to the source-page budget, which is also the author's call.

## Open: candidate findings fail the repository gate

`scripts/kp_check.py` reports `wiki health` red. Every error is on one of the
45 unreviewed candidates this run wrote; no promoted page has a finding,
because none has been promoted. That leaves a tension worth settling: the
ingest's contract says a candidate with findings is never promoted, which
treats findings on drafts as normal, while `kp_check` treats any wiki lint
error as a repository-level failure. As it stands, no draft can be in flight
without the gate being red. Whether candidate findings should gate promotion
only, or the repository too, is the author's call.
