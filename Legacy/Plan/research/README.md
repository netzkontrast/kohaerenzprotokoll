# Plan/research — open-access research downloads

Target directory of `scripts/research-tool.py` (vendored from alainator/worldcodex).
`papers/`, `markdown/`, `sep-entries/` and `last-search-results.json` are git-ignored;
`INDEX.md` (built by `python3 scripts/research-tool.py index`) is committed.
Usable findings are recorded in the graph with `capture_claim(text, source_uri, domain)`
and mapped onto the DKT via the `/integrating-research` skill.
