# Plan/queries — filed syntheses from `/query`

One file per high-value answer (`YYYY-MM-DD-<slug>.md`, frontmatter with
`title`, `tags`, `status: draft`, `source`, `query`). Only answers that
synthesised 3+ documents or resolved an ambiguity are filed; the author
promotes what should become canon or a codex entry.
